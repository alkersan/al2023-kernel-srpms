Date: Fri, 13 Jun 2025 18:47:16 +0000
Subject: AL2023 6.12: Update lustrefsx to 2.15.6-fsx19 commit


diff --git a/fs/lustre/config.h b/fs/lustre/config.h
--- a/fs/lustre/config.h
+++ b/fs/lustre/config.h
@@ -1075,9 +1075,6 @@
 /* new_sync_[read|write] is exported by the kernel */
 /* #undef HAVE_SYNC_READ_WRITE */
 
-/* sysfs_emit_at() is defined */
-#define HAVE_SYSFS_EMIT_AT 1
-
 /* Define to 1 if you have <sys/quota.h>. */
 #define HAVE_SYS_QUOTA_H 1
 
@@ -1289,7 +1286,7 @@
 #define LUSTRE_PATCH 6
 
 /* A copy of PACKAGE_VERSION */
-#define LUSTRE_VERSION_STRING "2.15.6_599_gd5769bb_dirty"
+#define LUSTRE_VERSION_STRING "2.15.6_648_g12ba450_dirty"
 
 /* maximum number of MDS threads */
 /* #undef MDS_MAX_THREADS */
@@ -1319,7 +1316,7 @@
 #define PACKAGE_NAME "Lustre"
 
 /* Define to the full name and version of this package. */
-#define PACKAGE_STRING "Lustre 2.15.6_599_gd5769bb_dirty"
+#define PACKAGE_STRING "Lustre 2.15.6_648_g12ba450_dirty"
 
 /* Define to the one symbol short name of this package. */
 #define PACKAGE_TARNAME "lustre"
@@ -1328,7 +1325,7 @@
 #define PACKAGE_URL ""
 
 /* Define to the version of this package. */
-#define PACKAGE_VERSION "2.15.6_599_gd5769bb_dirty"
+#define PACKAGE_VERSION "2.15.6_648_g12ba450_dirty"
 
 /* name of parallel fsck program */
 #define PFSCK "fsck"
@@ -1372,7 +1369,7 @@
 /* #undef USE_HEALTH_CHECK_WRITE */
 
 /* Version number of package */
-#define VERSION "2.15.6_599_gd5769bb_dirty"
+#define VERSION "2.15.6_648_g12ba450_dirty"
 
 /* vfs_setxattr() value argument is non-const */
 #define VFS_SETXATTR_VALUE(value) ((void *)(value))
diff --git a/fs/lustre/undef.h b/fs/lustre/undef.h
--- a/fs/lustre/undef.h
+++ b/fs/lustre/undef.h
@@ -1073,9 +1073,6 @@
 /* new_sync_[read|write] is exported by the kernel */
 #undef HAVE_SYNC_READ_WRITE
 
-/* sysfs_emit_at() is defined */
-#undef HAVE_SYSFS_EMIT_AT
-
 /* Define to 1 if you have <sys/quota.h>. */
 #undef HAVE_SYS_QUOTA_H
 
diff --git a/net/lnet/libcfs/include/libcfs/libcfs_private.h b/net/lnet/libcfs/include/libcfs/libcfs_private.h
--- a/net/lnet/libcfs/include/libcfs/libcfs_private.h
+++ b/net/lnet/libcfs/include/libcfs/libcfs_private.h
@@ -186,7 +186,6 @@ do {									    \
  * default allocator
  */
 #define LIBCFS_ALLOC(ptr, size) LIBCFS_ALLOC_GFP(ptr, size, GFP_NOFS)
-#define LIBCFS_ALLOC_PTR(ptr) LIBCFS_ALLOC(ptr, sizeof(*(ptr)))
 
 /**
  * non-sleeping allocator
@@ -230,8 +229,6 @@ do {									\
 		kfree(ptr);						\
 } while (0)
 
-#define LIBCFS_FREE_PTR(ptr) LIBCFS_FREE(ptr, sizeof(*(ptr)))
-
 #define LIBCFS_FREE_PRE(ptr, size, name)				\
 do {									\
 	LASSERT(ptr);							\
diff --git a/net/lnet/lnet/include/lnet/lib-lnet.h b/net/lnet/lnet/include/lnet/lib-lnet.h
--- a/net/lnet/lnet/include/lnet/lib-lnet.h
+++ b/net/lnet/lnet/include/lnet/lib-lnet.h
@@ -570,6 +570,8 @@ static inline void lnet_hdr_from_nid16(struct lnet_hdr *hdr,
 	hdr->src_pid = le32_to_cpu(hdr16->src_pid);
 	hdr->type = le32_to_cpu(hdr16->type);
 	hdr->payload_length = le32_to_cpu(hdr16->payload_length);
+
+	hdr->msg = hdr16->msg;
 }
 
 static inline void lnet_hdr_to_nid16(const struct lnet_hdr *hdr,
@@ -583,6 +585,8 @@ static inline void lnet_hdr_to_nid16(const struct lnet_hdr *hdr,
 	hdr16->src_pid = cpu_to_le32(hdr->src_pid);
 	hdr16->type = cpu_to_le32(hdr->type);
 	hdr16->payload_length = cpu_to_le32(hdr->payload_length);
+
+	hdr16->msg = hdr->msg;
 }
 
 extern const struct lnet_lnd the_lolnd;
@@ -706,7 +710,7 @@ void lnet_return_tx_credits_locked(struct lnet_msg *msg);
 void lnet_return_rx_credits_locked(struct lnet_msg *msg);
 void lnet_schedule_blocked_locked(struct lnet_rtrbufpool *rbp);
 void lnet_drop_routed_msgs_locked(struct list_head *list, int cpt);
-int lnet_discover_nid_metadata(lnet_nid_t nid, struct lnet_nid_metadata *mapping);
+int lnet_discover_nid_metadata(lnet_nid_t nid, struct lnet_nid_metadata *data);
 
 struct list_head **lnet_create_array_of_queues(void);
 
diff --git a/net/lnet/lnet/include/uapi/linux/lnet/lnet-idl.h b/net/lnet/lnet/include/uapi/linux/lnet/lnet-idl.h
--- a/net/lnet/lnet/include/uapi/linux/lnet/lnet-idl.h
+++ b/net/lnet/lnet/include/uapi/linux/lnet/lnet-idl.h
@@ -260,10 +260,7 @@ struct lnet_ni_status {
 
 struct lnet_nid_md_entry {
 	lnet_nid_t nid;
-	__u64 buffer;
-	__u64 buffer_1;
-	__u64 buffer_2;
-	__u64 buffer_end;
+	__u8 buffer[32];
 } __attribute__((packed));
 
 struct lnet_nid_metadata {
diff --git a/net/lnet/lnet/include/uapi/linux/lnet/lnet-types.h b/net/lnet/lnet/include/uapi/linux/lnet/lnet-types.h
--- a/net/lnet/lnet/include/uapi/linux/lnet/lnet-types.h
+++ b/net/lnet/lnet/include/uapi/linux/lnet/lnet-types.h
@@ -162,7 +162,7 @@ static inline int nid_same(const struct lnet_nid *n1,
 		n1->nid_addr[3] == n2->nid_addr[3];
 }
 
-static inline int nid_net_same(const struct lnet_nid *n1,
+static inline int nid_same_net(const struct lnet_nid *n1,
 			       const struct lnet_nid *n2)
 {
 	return n1->nid_size == n2->nid_size &&
diff --git a/net/lnet/lnet/klnds/efalnd/Makefile b/net/lnet/lnet/klnds/efalnd/Makefile
--- a/net/lnet/lnet/klnds/efalnd/Makefile
+++ b/net/lnet/lnet/klnds/efalnd/Makefile
@@ -1,5 +1,5 @@
 obj-$(CONFIG_LUSTRE_LNET_EFALND)	+= kefalnd.o
 
-kefalnd-y		:= efalnd.o efalnd_connection.o efalnd_modparams.o efalnd_peerni.o
+kefalnd-y		:= efalnd.o efalnd_connection.o efalnd_modparams.o efalnd_peerni.o efalnd_debugfs.o
 
 include $(srctree)/net/lnet/Makefile.rules
diff --git a/net/lnet/lnet/klnds/efalnd/efalnd.c b/net/lnet/lnet/klnds/efalnd/efalnd.c
--- a/net/lnet/lnet/klnds/efalnd/efalnd.c
+++ b/net/lnet/lnet/klnds/efalnd/efalnd.c
@@ -1,7 +1,7 @@
 // SPDX-License-Identifier: GPL-2.0
 
 /*
- * Copyright (c) 2023-2024, Amazon and/or its affiliates. All rights reserved.
+ * Copyright (c) 2023-2025, Amazon and/or its affiliates. All rights reserved.
  * Use is subject to license terms.
  */
 
@@ -38,23 +38,11 @@ struct kefa_data kefalnd;
 	__stringify(EFALND_SUBMINOR_VER)
 #endif
 
-/* TODO - move to tunable. consider device attributes */
 #define MAX_CQE_BATCH	16
 #define SQ_DEPTH	4096
 #define RQ_DEPTH	4096
 #define CQ_DEPTH	(SQ_DEPTH + RQ_DEPTH)
 
-#ifdef KEFALND_DEBUG
-static void kefalnd_hexdump_buf(char *prefix, void *buf, int len)
-{
-	print_hex_dump(KERN_INFO, prefix, DUMP_PREFIX_OFFSET, 16, 1, buf, len, true);
-}
-#else
-static void kefalnd_hexdump_buf(char *prefix, void *buf, int len)
-{
-}
-#endif
-
 static char *
 kefalnd_msgtype2str(int type)
 {
@@ -79,14 +67,14 @@ kefalnd_msgtype2str(int type)
 	return msg_name[type];
 }
 
-static int
+int
 kefalnd_msgtype2size(int type)
 {
-	const int hdr_size = offsetof(struct kefa_msg, u);
+	const int hdr_size = offsetof(struct kefa_msg, msg_v1.u);
 
 	switch (type) {
 	case EFALND_MSG_IMMEDIATE:
-		return offsetof(struct kefa_msg, u.immediate.payload[0]);
+		return offsetof(struct kefa_msg, msg_v1.u.immediate.payload[0]);
 
 	case EFALND_MSG_PUTR_REQ:
 		return hdr_size + sizeof(struct kefa_putr_req_msg);
@@ -103,13 +91,14 @@ kefalnd_msgtype2size(int type)
 		return hdr_size + sizeof(struct kefa_conn_probe_msg);
 
 	case EFALND_MSG_CONN_PROBE_RESP:
-		return hdr_size + sizeof(struct kefa_conn_probe_resp_msg);
+		/* min/max proto version are optional */
+		return offsetof(struct kefa_msg, msg_v1.u.conn_probe_resp.caps);
 
 	case EFALND_MSG_CONN_REQ:
-		return offsetof(struct kefa_msg, u.conn_request.data_qps[0]);
+		return offsetof(struct kefa_msg, msg_v1.u.conn_request.data_qps[0]);
 
 	case EFALND_MSG_CONN_REQ_ACK:
-		return offsetof(struct kefa_msg, u.conn_request_ack.data_qps[0]);
+		return offsetof(struct kefa_msg, msg_v1.u.conn_request_ack.data_qps[0]);
 
 	default:
 		return -1;
@@ -194,6 +183,24 @@ kefalnd_get_dev_prio(struct lnet_ni *ni, unsigned int dev_idx)
 	return lnet_get_dev_prio(dev, dev_idx);
 }
 
+void
+kefalnd_get_srcnid_from_msg(struct kefa_msg *msg, struct lnet_nid *srcnid)
+{
+	if (msg->hdr.proto_ver != EFALND_PROTO_VER_1)
+		*srcnid = msg->msg_v2.srcnid;
+	else
+		lnet_nid4_to_nid(msg->msg_v1.srcnid, srcnid);
+}
+
+void
+kefalnd_get_dstnid_from_msg(struct kefa_msg *msg, struct lnet_nid *dstnid)
+{
+	if (msg->hdr.proto_ver != EFALND_PROTO_VER_1)
+		*dstnid = msg->msg_v2.dstnid;
+	else
+		lnet_nid4_to_nid(msg->msg_v1.dstnid, dstnid);
+}
+
 static int
 kefalnd_obj_pool_init(struct kefa_ni *efa_ni, struct kefa_obj_pool *pool, u32 pool_size,
 		      int cpt, size_t obj_size)
@@ -321,7 +328,7 @@ kefalnd_get_tx_by_idx(struct kefa_ni *efa_ni, u64 tx_idx)
 	struct kefa_obj_pool *tx_pool = &efa_ni->tx_pool;
 
 	if (tx_idx >= tx_pool->pool_size) {
-		EFA_DEV_ERR(efa_ni->efa_dev, "received out of range TX[%llu] max[%u]",
+		EFA_DEV_ERR(efa_ni->efa_dev, "received out of range TX[%llu] max[%u]\n",
 			    tx_idx, tx_pool->pool_size);
 		return NULL;
 	}
@@ -330,7 +337,7 @@ kefalnd_get_tx_by_idx(struct kefa_ni *efa_ni, u64 tx_idx)
 }
 
 static void
-kefalnd_init_tx_sge(struct kefa_tx *tx, u32 lkey, u64 addr, unsigned int len)
+kefalnd_init_tx_protocol_sge(struct kefa_tx *tx, u32 lkey, u64 addr, unsigned int len)
 {
 	struct ib_sge *sge = &tx->sge;
 
@@ -342,9 +349,9 @@ kefalnd_init_tx_sge(struct kefa_tx *tx, u32 lkey, u64 addr, unsigned int len)
 }
 
 void
-kefalnd_init_tx_msg(struct kefa_tx *tx, int type, int body_nob)
+kefalnd_init_tx_protocol_msg(struct kefa_tx *tx, int type, int body_nob)
 {
-	int nob = offsetof(struct kefa_msg, u) + body_nob;
+	int nob = offsetof(struct kefa_msg, msg_v1.u) + body_nob;
 	struct ib_srd_rdma_wr *wrq;
 
 	LASSERT(nob <= EFALND_MSG_SIZE);
@@ -352,7 +359,6 @@ kefalnd_init_tx_msg(struct kefa_tx *tx, int type, int body_nob)
 	tx->type = type;
 	kefalnd_init_msg(tx->msg, type, body_nob);
 
-	/* TODO - support multiple WQE per TX */
 	wrq = &tx->wrq;
 	*wrq = (struct ib_srd_rdma_wr) {
 		.wr.wr = {
@@ -365,7 +371,7 @@ kefalnd_init_tx_msg(struct kefa_tx *tx, int type, int body_nob)
 		},
 	};
 
-	kefalnd_init_tx_sge(tx, tx->lkey, tx->msgaddr, nob);
+	kefalnd_init_tx_protocol_sge(tx, tx->lkey, tx->msgaddr, nob);
 }
 
 static struct kefa_remote_qp *
@@ -413,10 +419,8 @@ __must_hold(&conn->lock)
 			wr = &tx->wrq.wr.wr;
 			kefalnd_set_tx_remote_data(conn, tx);
 			kefalnd_msg_fill_hdr(conn, tx->msg, conn->proto_ver);
-			kefalnd_hexdump_buf("tx-buf: ", tx->msg, sizeof(*tx->msg));
 		}
 
-		tx->qp = qp;
 		atomic64_set(&tx->send_time, now);
 		atomic_inc(&tx->ref_cnt);
 		list_move_tail(&tx->list_node, &conn->active_tx);
@@ -428,7 +432,6 @@ __must_hold(&conn->lock)
 					     qp->ib_qp->qp_num, rc);
 			}
 
-			tx->qp = NULL;
 			atomic64_set(&tx->send_time, 0);
 			atomic_dec(&tx->ref_cnt);
 			list_move(&tx->list_node, &conn->pend_tx);
@@ -438,11 +441,6 @@ __must_hold(&conn->lock)
 			 */
 			break;
 		}
-
-		/* TT: https://issues.amazon.com/issues/EFA-3459 */
-		rc = ib_req_notify_cq(qp->cq->ib_cq, IB_CQ_NEXT_COMP);
-		if (rc < 0)
-			EFA_DEV_ERR(qp->cq->efa_dev, "request notify CQ failed. err[%d]\n", rc);
 	}
 }
 
@@ -474,8 +472,6 @@ kefalnd_post_finv_failure(struct kefa_dev *efa_dev, struct kefa_qp *qp,
 		EFA_DEV_WARN(efa_dev, "QP[%u] failed to post FINV[0x%x]. err[%d]\n",
 			     qp->ib_qp->qp_num, fmr->mr->lkey, rc);
 	}
-
-	fmr->inv_qp = NULL;
 }
 
 static void
@@ -492,7 +488,6 @@ kefalnd_launch_pending_finvs(struct kefa_dev *efa_dev)
 	list_for_each_entry_safe(fmr, temp_fmr, &fmr_pool->free_pend_obj, list_node) {
 		wr = &fmr->inv_wr;
 		qp = kefalnd_device_get_qp(efa_dev);
-		fmr->inv_qp = qp;
 		list_del_init(&fmr->list_node);
 		rc = ib_post_send(qp->ib_qp, wr, NULL);
 		if (rc) {
@@ -517,7 +512,6 @@ kefalnd_launch_finv(struct kefa_dev *efa_dev, struct kefa_fmr *fmr)
 
 	wr = &fmr->inv_wr;
 	qp = kefalnd_device_get_qp(efa_dev);
-	fmr->inv_qp = qp;
 	rc = ib_post_send(qp->ib_qp, wr, NULL);
 	if (rc) {
 		kefalnd_post_finv_failure(efa_dev, qp, fmr, rc);
@@ -554,15 +548,16 @@ kefalnd_unmap_tx(struct kefa_tx *tx)
 }
 
 static int
-kefalnd_map_tx(struct kefa_ni *efa_ni, struct kefa_tx *tx,
-	       struct kefa_rdma_desc *rd)
+kefalnd_map_tx(struct kefa_ni *efa_ni, struct kefa_tx *tx, struct kefa_rdma_desc *rd,
+	       bool remote_access_fmr)
 {
 	struct kefa_dev *efa_dev = efa_ni->efa_dev;
 	int i, sg_nsegs, fmr_nsegs;
 	struct ib_send_wr *inv_wr;
 	struct ib_reg_wr *reg_wr;
-	__u32 nob;
+	u32 nob;
 
+	tx->dmadir = remote_access_fmr ? DMA_TO_DEVICE : DMA_FROM_DEVICE;
 	sg_nsegs = kefalnd_dma_map_sg(efa_dev, tx->frags, tx->nfrags, tx->dmadir);
 	if (unlikely(sg_nsegs < 0)) {
 		EFA_DEV_ERR(efa_dev, "Failed to DMA map TX, err %d\n", sg_nsegs);
@@ -593,7 +588,9 @@ kefalnd_map_tx(struct kefa_ni *efa_ni, struct kefa_tx *tx,
 	reg_wr->wr.send_flags = 0;
 	reg_wr->mr = tx->fmr->mr;
 	reg_wr->key = tx->fmr->mr->lkey;
-	reg_wr->access = (IB_ACCESS_LOCAL_WRITE | IB_ACCESS_REMOTE_READ);
+	reg_wr->access = IB_ACCESS_LOCAL_WRITE;
+	if (remote_access_fmr)
+		reg_wr->access |= IB_ACCESS_REMOTE_READ;
 
 	inv_wr = &tx->fmr->inv_wr;
 	memset(inv_wr, 0, sizeof(*inv_wr));
@@ -601,29 +598,21 @@ kefalnd_map_tx(struct kefa_ni *efa_ni, struct kefa_tx *tx,
 	inv_wr->wr_id  = (u64)tx->fmr;
 	inv_wr->ex.invalidate_rkey = tx->fmr->mr->lkey;
 
-	rd->frag.nob = nob;
-	rd->frag.addr = sg_dma_address(&tx->frags[0]);
-	rd->key = tx->fmr->mr->rkey;
+	rd->nob = nob;
+	rd->addr = sg_dma_address(&tx->frags[0]);
+	rd->key = remote_access_fmr ? tx->fmr->mr->rkey : tx->fmr->mr->lkey;
 
 	return 0;
 }
 
 static int
-kefalnd_map_msg_iov(struct kefa_ni *efa_ni, struct kefa_tx *tx,
-		    struct kefa_rdma_desc *rd, int nkiov,
-		    struct bio_vec *kiov, int offset, int nob)
+kefalnd_bio_vec_to_sgl(struct kefa_ni *efa_ni, struct scatterlist *sg, struct bio_vec *kiov,
+		       int nkiov, int offset, int nob)
 {
-	struct scatterlist *sg;
-	int fragnob;
-	int max_nkiov;
-	int sg_count = 0;
-
-	CDEBUG(D_NET, "niov %d offset %d nob %d\n", nkiov, offset, nob);
+	int fragnob, max_nkiov, sg_count = 0;
 
 	LASSERT(nob > 0);
 	LASSERT(nkiov > 0);
-	LASSERT(efa_ni);
-	LASSERT(tx->dmadir != DMA_BIDIRECTIONAL);
 
 	/* Trasnalate from bio_vec to sg to use for mapping */
 	while (offset >= kiov->bv_len) {
@@ -634,13 +623,11 @@ kefalnd_map_msg_iov(struct kefa_ni *efa_ni, struct kefa_tx *tx,
 	}
 
 	max_nkiov = nkiov;
-
-	sg = tx->frags;
 	do {
 		LASSERT(nkiov > 0);
 
 		if (!sg) {
-			EFA_DEV_ERR(efa_ni->efa_dev, "lacking enough sg entries to map tx\n");
+			EFA_DEV_ERR(efa_ni->efa_dev, "lacking enough sg entries to map TX\n");
 			return -EFAULT;
 		}
 		sg_count++;
@@ -671,27 +658,35 @@ kefalnd_map_msg_iov(struct kefa_ni *efa_ni, struct kefa_tx *tx,
 		nob -= fragnob;
 	} while (nob > 0);
 
-	tx->nfrags = sg_count;
+	return sg_count;
+}
+
+static int
+kefalnd_map_msg_iov(struct kefa_ni *efa_ni, struct kefa_tx *tx, struct kefa_rdma_desc *rd,
+		    int nkiov, struct bio_vec *kiov, int offset, int nob, bool remote_access_fmr)
+{
+	tx->nfrags = kefalnd_bio_vec_to_sgl(efa_ni, tx->frags, kiov, nkiov, offset, nob);
+	if (tx->nfrags < 0)
+		return tx->nfrags;
 
 	/* Map the SGs to our device */
-	return kefalnd_map_tx(efa_ni, tx, rd);
+	return kefalnd_map_tx(efa_ni, tx, rd, remote_access_fmr);
 }
 
 static void
 kefalnd_init_comp_message(struct kefa_tx *tx, int type, int status, u64 cookie)
 {
-	kefalnd_init_tx_msg(tx, type, sizeof(struct kefa_completion_msg));
+	kefalnd_init_tx_protocol_msg(tx, type, sizeof(struct kefa_completion_msg));
 
-	tx->msg->u.completion.cookie = cookie;
-	tx->msg->u.completion.status = kefalnd_errno_to_efa_status(status);
+	tx->msg->msg_v1.u.completion.cookie = cookie;
+	tx->msg->msg_v1.u.completion.status = kefalnd_errno_to_efa_status(status);
 }
 
-static void
-kefalnd_set_sync_data(struct kefa_tx *tx, u64 cookie, u8 type)
+static inline void
+kefalnd_set_sync_data(struct kefa_tx *tx, u64 cookie)
 {
 	tx->send_sync = true;
 	tx->cookie = cookie;
-	tx->type = type;
 }
 
 static void
@@ -708,11 +703,28 @@ kefalnd_send_sync_msg(struct kefa_tx *tx)
 	kefalnd_launch_tx(tx->conn, tx);
 }
 
-static void
-kefalnd_tx_done_common(struct kefa_tx *tx)
+void
+kefalnd_tx_done(struct kefa_tx *tx)
 {
+	unsigned long flags;
 	int i;
 
+	/* Send last control message once all RDMAs completed.
+	 * re-use the connection and message types set during RDMA submit
+	 */
+	if (tx->send_sync) {
+		kefalnd_send_sync_msg(tx);
+		return;
+	}
+
+	if (tx->conn) {
+		spin_lock_irqsave(&tx->conn->lock, flags);
+		if (!list_empty(&tx->list_node))
+			list_del_init(&tx->list_node);
+
+		spin_unlock_irqrestore(&tx->conn->lock, flags);
+	}
+
 	CDEBUG(D_NET, "Completed TX type[%s] from[%s] to[%s]\n",
 	       kefalnd_msgtype2str(tx->type),
 	       tx->conn ? libcfs_nidstr(&tx->conn->local_nid) : "NA",
@@ -742,57 +754,14 @@ kefalnd_tx_done_common(struct kefa_tx *tx)
 
 	tx->hstatus = LNET_MSG_STATUS_OK;
 	tx->conn = NULL;
-	tx->qp = NULL;
 	tx->dmadir = DMA_BIDIRECTIONAL;
 	tx->type = 0;
 
 	kefalnd_obj_pool_free(tx->tx_pool, &tx->list_node);
 }
 
-static void
-kefalnd_tx_done_locked(struct kefa_tx *tx)
-__must_hold(&tx->conn->lock)
-{
-	/* Send last control message once all RDMAs completed.
-	 * re-use the connection and message types set during RDMA submit
-	 */
-	if (tx->send_sync) {
-		kefalnd_send_sync_msg(tx);
-		return;
-	}
-
-	if (!list_empty(&tx->list_node))
-		list_del_init(&tx->list_node);
-
-	kefalnd_tx_done_common(tx);
-}
-
 void
-kefalnd_tx_done(struct kefa_tx *tx)
-{
-	unsigned long flags;
-
-	/* Send last control message once all RDMAs completed.
-	 * re-use the connection and message types set during RDMA submit
-	 */
-	if (tx->send_sync) {
-		kefalnd_send_sync_msg(tx);
-		return;
-	}
-
-	if (tx->conn) {
-		spin_lock_irqsave(&tx->conn->lock, flags);
-		if (!list_empty(&tx->list_node))
-			list_del_init(&tx->list_node);
-
-		spin_unlock_irqrestore(&tx->conn->lock, flags);
-	}
-
-	kefalnd_tx_done_common(tx);
-}
-
-static bool
-kefalnd_abort_tx_common(struct kefa_tx *tx, enum lnet_msg_hstatus hstatus, int status)
+kefalnd_abort_tx(struct kefa_tx *tx, enum lnet_msg_hstatus hstatus, int status)
 {
 	EFA_DEV_WARN(tx->conn->efa_ni->efa_dev, "aborting TX type[%s] to peer NI[%s]\n",
 		     kefalnd_msgtype2str(tx->type), libcfs_nidstr(&tx->conn->remote_nid));
@@ -803,30 +772,14 @@ kefalnd_abort_tx_common(struct kefa_tx *tx, enum lnet_msg_hstatus hstatus, int s
 
 	/* Make sure response message refcount decreased only once */
 	if (!atomic_xchg_relaxed(&tx->waiting_resp, false))
-		return false;
-
-	/* We have been waiting for response, don't need to now */
-	return atomic_dec_and_test(&tx->ref_cnt);
-}
-
-void
-kefalnd_abort_tx_locked(struct kefa_tx *tx, enum lnet_msg_hstatus hstatus, int status)
-__must_hold(&tx->conn->lock)
-{
-	if (kefalnd_abort_tx_common(tx, hstatus, status))
-		kefalnd_tx_done_locked(tx);
-}
+		return;
 
-static void
-kefalnd_abort_tx(struct kefa_tx *tx, enum lnet_msg_hstatus hstatus, int status)
-{
-	if (kefalnd_abort_tx_common(tx, hstatus, status))
+	if (atomic_dec_and_test(&tx->ref_cnt))
 		kefalnd_tx_done(tx);
 }
 
 void
-kefalnd_force_cancel_tx_locked(struct kefa_tx *tx, enum lnet_msg_hstatus hstatus, int status)
-__must_hold(&tx->conn->lock)
+kefalnd_force_cancel_tx(struct kefa_tx *tx, enum lnet_msg_hstatus hstatus, int status)
 {
 	EFA_DEV_WARN(tx->conn->efa_ni->efa_dev, "canceling TX type[%s] to peer NI[%s]\n",
 		     kefalnd_msgtype2str(tx->type), libcfs_nidstr(&tx->conn->remote_nid));
@@ -838,21 +791,19 @@ __must_hold(&tx->conn->lock)
 	atomic_set(&tx->waiting_resp, false);
 	atomic_set(&tx->ref_cnt, 0);
 
-	kefalnd_tx_done_locked(tx);
+	kefalnd_tx_done(tx);
 }
 
 static void
 kefalnd_send_completion(struct kefa_ni *efa_ni, struct kefa_conn *conn,
 			int type, int status, u64 cookie)
 {
-	struct lnet_nid *target_nid;
 	struct kefa_tx *tx;
 
 	tx = kefalnd_get_idle_tx(efa_ni);
 	if (tx == NULL) {
-		target_nid = &conn->remote_nid;
-		EFA_DEV_ERR(efa_ni->efa_dev, "can't allocate %s completion txd for peer NI[%s]\n",
-			    lnet_msgtyp2str(type), libcfs_nidstr(target_nid));
+		EFA_DEV_ERR(efa_ni->efa_dev, "can't allocate %s completion TX to peer NI[%s]\n",
+			    kefalnd_msgtype2str(type), libcfs_nidstr(&conn->remote_nid));
 
 		return;
 	}
@@ -865,38 +816,37 @@ kefalnd_send_completion(struct kefa_ni *efa_ni, struct kefa_conn *conn,
 static int
 kefalnd_send(struct lnet_ni *ni, void *private, struct lnet_msg *lntmsg)
 {
-	struct kefa_ni *efa_ni = ni->ni_data;
 	struct lnet_processid *target = &lntmsg->msg_target;
+	struct lnet_libmd *msg_md = lntmsg->msg_md;
 	struct lnet_hdr *hdr = &lntmsg->msg_hdr;
-	unsigned int payload_nob = lntmsg->msg_len;
-	unsigned int payload_niov = lntmsg->msg_niov;
-	unsigned int payload_offset = lntmsg->msg_offset;
-	struct bio_vec *payload_kiov = lntmsg->msg_kiov;
+	struct kefa_ni *efa_ni = ni->ni_data;
 	int type = lntmsg->msg_type;
+	struct kefa_msg_v1 *msg_v1;
 	struct kefa_rdma_desc *rd;
 	struct kefa_conn *conn;
-	struct kefa_msg *msg;
 	struct kefa_tx *tx;
 	int nob, rc;
 
 	tx = kefalnd_get_idle_tx(efa_ni);
 	if (tx == NULL) {
-		EFA_DEV_ERR(efa_ni->efa_dev, "can't allocate %s txd\n", lnet_msgtyp2str(type));
+		EFA_DEV_ERR(efa_ni->efa_dev, "can't allocate %s TX to peer NI[%s]\n",
+			    lnet_msgtyp2str(type), libcfs_nidstr(&target->nid));
+
 		return -ENOMEM;
 	}
-	msg = tx->msg;
+	msg_v1 = &tx->msg->msg_v1;
 
 	conn = kefalnd_lookup_or_init_conn(efa_ni, &target->nid, KEFA_CONN_TYPE_INITIATOR);
 	if (IS_ERR(conn)) {
-		EFA_DEV_ERR(efa_ni->efa_dev, "can't esatblish connection to peer NI[%s]\n",
-			    libcfs_nidstr(&target->nid));
-
+		EFA_DEV_DEBUG(efa_ni->efa_dev, "can't establish connection to peer NI[%s]\n",
+			      libcfs_nidstr(&target->nid));
+		kefalnd_tx_done(tx);
 		return -ENOTCONN;
 	}
 
 	CDEBUG(D_NET, "Request to send LNet %s from %s to %s size[%u]\n",
 	       lnet_msgtyp2str(type), libcfs_nidstr(&ni->ni_nid), libcfs_nidstr(&target->nid),
-	       payload_nob);
+	       type == LNET_MSG_GET ? msg_md->md_length : lntmsg->msg_len);
 
 	switch (type) {
 	default:
@@ -904,20 +854,18 @@ kefalnd_send(struct lnet_ni *ni, void *private, struct lnet_msg *lntmsg)
 		return (-EIO);
 
 	case LNET_MSG_ACK:
-		LASSERT (payload_nob == 0);
+		LASSERT(lntmsg->msg_len == 0);
 		break;
 
 	case LNET_MSG_GET:
 		/* use RDMA or SEND based on size */
-		nob = offsetof(struct kefa_msg, u.immediate.payload[lntmsg->msg_md->md_length]);
+		nob = offsetof(struct kefa_msg, msg_v1.u.immediate.payload[msg_md->md_length]);
 		if (nob <= EFALND_NO_RDMA_THRESH && !lntmsg->msg_rdma_force)
 			break;
 
 		/* RDMA based flow */
-		/* TODO - move this to GET_ACK handling */
-		tx->dmadir = DMA_FROM_DEVICE;
-		rc = kefalnd_map_msg_iov(efa_ni, tx, &tx->rdma_desc, lntmsg->msg_md->md_niov,
-					 lntmsg->msg_md->md_kiov, 0, lntmsg->msg_md->md_length);
+		rc = kefalnd_map_msg_iov(efa_ni, tx, &tx->rdma_desc, msg_md->md_niov,
+					 msg_md->md_kiov, 0, msg_md->md_length, false);
 		if (rc != 0) {
 			EFA_DEV_ERR(efa_ni->efa_dev,
 				    "can't setup GET destination for peer NI[%s]. err[%d]\n",
@@ -926,13 +874,13 @@ kefalnd_send(struct lnet_ni *ni, void *private, struct lnet_msg *lntmsg)
 			kefalnd_tx_done(tx);
 			return -EIO;
 		}
-		//kefalnd_rdma_dump(&msg->u.putr_req.rdma_desc);
 
 		/* setup the message */
-		lnet_hdr_to_nid4(hdr, &msg->u.getr_req.hdr);
-		kefalnd_init_tx_msg(tx, EFALND_MSG_GETR_REQ, sizeof(struct kefa_getr_req_msg));
+		lnet_hdr_to_nid4(hdr, &msg_v1->u.getr_req.hdr);
+		kefalnd_init_tx_protocol_msg(tx, EFALND_MSG_GETR_REQ,
+					     sizeof(struct kefa_getr_req_msg));
 
-		msg->u.getr_req.sink_cookie = kefalnd_tx_to_idx(tx);
+		msg_v1->u.getr_req.sink_cookie = kefalnd_tx_to_idx(tx);
 		tx->lntmsg[1] = lnet_create_reply_msg(ni, lntmsg);
 		if (tx->lntmsg[1] == NULL) {
 			EFA_DEV_ERR(efa_ni->efa_dev, "can't create reply for GET for peer NI[%s]\n",
@@ -952,15 +900,14 @@ kefalnd_send(struct lnet_ni *ni, void *private, struct lnet_msg *lntmsg)
 	case LNET_MSG_REPLY:
 	case LNET_MSG_PUT:
 		/* use RDMA or SEND based on size */
-		nob = offsetof(struct kefa_msg, u.immediate.payload[payload_nob]);
+		nob = offsetof(struct kefa_msg, msg_v1.u.immediate.payload[lntmsg->msg_len]);
 		if (nob <= EFALND_NO_RDMA_THRESH && !lntmsg->msg_rdma_force)
 			break;
 
 		/* RDMA based flow */
-		rd = &msg->u.putr_req.rdma_desc;
-		tx->dmadir = DMA_TO_DEVICE;
-		rc = kefalnd_map_msg_iov(efa_ni, tx, rd, payload_niov, payload_kiov,
-					 payload_offset, payload_nob);
+		rd = &msg_v1->u.putr_req.rdma_desc;
+		rc = kefalnd_map_msg_iov(efa_ni, tx, rd, lntmsg->msg_niov, lntmsg->msg_kiov,
+					 lntmsg->msg_offset, lntmsg->msg_len, true);
 		if (rc != 0) {
 			EFA_DEV_ERR(efa_ni->efa_dev,
 				    "can't setup PUT src for peer NI[%s]. err[%d]\n",
@@ -969,13 +916,13 @@ kefalnd_send(struct lnet_ni *ni, void *private, struct lnet_msg *lntmsg)
 			kefalnd_tx_done(tx);
 			return -EIO;
 		}
-		//kefalnd_rdma_dump(&msg->u.putr_req.rdma_desc);
 
 		/* setup the message */
-		lnet_hdr_to_nid4(hdr, &msg->u.putr_req.hdr);
-		kefalnd_init_tx_msg(tx, EFALND_MSG_PUTR_REQ, sizeof(struct kefa_putr_req_msg));
+		lnet_hdr_to_nid4(hdr, &msg_v1->u.putr_req.hdr);
+		kefalnd_init_tx_protocol_msg(tx, EFALND_MSG_PUTR_REQ,
+					     sizeof(struct kefa_putr_req_msg));
 
-		msg->u.putr_req.cookie = kefalnd_tx_to_idx(tx);
+		msg_v1->u.putr_req.cookie = kefalnd_tx_to_idx(tx);
 		/* finalise lntmsg[0,1] on completion */
 		tx->lntmsg[0] = lntmsg;
 		atomic_inc(&tx->ref_cnt); /* wait for PUT_DONE */
@@ -985,16 +932,13 @@ kefalnd_send(struct lnet_ni *ni, void *private, struct lnet_msg *lntmsg)
 	}
 
 	/* SEND based (non-RDMA flow) */
-	msg = tx->msg;
-	lnet_hdr_to_nid4(hdr, &msg->u.immediate.hdr);
+	lnet_hdr_to_nid4(hdr, &msg_v1->u.immediate.hdr);
+	lnet_copy_kiov2flat(EFALND_MSG_SIZE, tx->msg,
+			    offsetof(struct kefa_msg, msg_v1.u.immediate.payload), lntmsg->msg_niov,
+			    lntmsg->msg_kiov, lntmsg->msg_offset, lntmsg->msg_len);
 
-	/* TODO - can't we send the KIOV directly ? */
-	lnet_copy_kiov2flat(EFALND_MSG_SIZE, msg,
-			    offsetof(struct kefa_msg, u.immediate.payload),
-			    payload_niov, payload_kiov, payload_offset, payload_nob);
-
-	nob = offsetof(struct kefa_immediate_msg, payload[payload_nob]);
-	kefalnd_init_tx_msg(tx, EFALND_MSG_IMMEDIATE, nob);
+	nob = offsetof(struct kefa_immediate_msg, payload[lntmsg->msg_len]);
+	kefalnd_init_tx_protocol_msg(tx, EFALND_MSG_IMMEDIATE, nob);
 
 	/* finalise lntmsg on completion */
 	tx->lntmsg[0] = lntmsg;
@@ -1005,8 +949,8 @@ kefalnd_send(struct lnet_ni *ni, void *private, struct lnet_msg *lntmsg)
 }
 
 static void
-kefalnd_init_rdma_read(struct kefa_conn *conn, struct kefa_tx *tx, int type,
-		       struct kefa_rdma_desc *src_rdma, u64 dstcookie)
+kefalnd_init_tx_rdma_read(struct kefa_conn *conn, struct kefa_tx *tx, int type,
+			  struct kefa_rdma_desc *src_rdma, u64 dstcookie)
 {
 	struct kefa_rdma_desc *sink_rdma = &tx->rdma_desc;
 	struct ib_srd_rdma_wr *wrq;
@@ -1017,13 +961,15 @@ kefalnd_init_rdma_read(struct kefa_conn *conn, struct kefa_tx *tx, int type,
 	LASSERT(!in_interrupt());
 	LASSERT(type == EFALND_MSG_PUTR_DONE || type == EFALND_MSG_GETR_DONE);
 
-	sge_nob = min(kefalnd_rdma_frag_size(src_rdma), kefalnd_rdma_frag_size(sink_rdma));
+	tx->type = type;
+
+	sge_nob = min(src_rdma->nob, sink_rdma->nob);
 
 	sge = &tx->sge;
 	wrq = &tx->wrq;
 
-	sge->addr = kefalnd_rdma_frag_addr(sink_rdma);
-	sge->lkey = kefalnd_rdma_frag_key(sink_rdma);
+	sge->addr = sink_rdma->addr;
+	sge->lkey = sink_rdma->key;
 	sge->length = sge_nob;
 
 	ib_wr = &wrq->wr.wr;
@@ -1036,51 +982,47 @@ kefalnd_init_rdma_read(struct kefa_conn *conn, struct kefa_tx *tx, int type,
 	ib_wr->send_flags = 0;
 
 	/* RDMA specific */
-	wrq->remote_addr = kefalnd_rdma_frag_addr(src_rdma);
-	wrq->rkey = kefalnd_rdma_frag_key(src_rdma);
+	wrq->remote_addr = src_rdma->addr;
+	wrq->rkey = src_rdma->key;
 
-	kefalnd_set_sync_data(tx, dstcookie, type);
+	kefalnd_set_sync_data(tx, dstcookie);
 }
 
 static int
-kefalnd_handle_putr_req(struct kefa_ni *efa_ni, struct kefa_conn *conn, struct kefa_rx *rx,
-			struct lnet_msg *lntmsg, int nob)
+kefalnd_handle_putr_req(struct kefa_ni *efa_ni, struct kefa_conn *conn, struct lnet_msg *lntmsg,
+			struct kefa_rdma_desc *src_rd, u64 src_cookie, int nob)
 {
-	u64 cookie = rx->msg->u.putr_req.cookie;
 	struct kefa_tx *tx;
 	int rc = 0;
 
 	if (lntmsg == NULL) {
-		kefalnd_send_completion(efa_ni, conn, EFALND_MSG_PUTR_DONE,
-					-ENODATA, cookie);
+		kefalnd_send_completion(efa_ni, conn, EFALND_MSG_PUTR_DONE, -ENODATA, src_cookie);
 		return 0;
 	}
 
 	tx = kefalnd_get_idle_tx(efa_ni);
 	if (tx == NULL) {
-		EFA_DEV_ERR(efa_ni->efa_dev, "can't get tx for %s to peer NI[%s]\n",
-			    kefalnd_msgtype2str(rx->msg->type), libcfs_nidstr(&conn->remote_nid));
+		EFA_DEV_ERR(efa_ni->efa_dev, "can't allocate %s TX to peer NI[%s]\n",
+			    kefalnd_msgtype2str(EFALND_MSG_PUTR_DONE),
+			    libcfs_nidstr(&conn->remote_nid));
 
 		return -ENOMEM;
 	}
 
-	tx->dmadir = DMA_FROM_DEVICE;
-
 	if (likely(nob != 0))
 		rc = kefalnd_map_msg_iov(efa_ni, tx, &tx->rdma_desc, lntmsg->msg_niov,
-					 lntmsg->msg_kiov, lntmsg->msg_offset, nob);
+					 lntmsg->msg_kiov, lntmsg->msg_offset, nob, false);
 	if (unlikely(rc != 0)) {
 		EFA_DEV_ERR(efa_ni->efa_dev, "can't setup GET src for peer NI[%s]. err[%d]\n",
 			    libcfs_nidstr(&conn->remote_nid), rc);
 
 		tx->hstatus = LNET_MSG_STATUS_LOCAL_ERROR;
 		kefalnd_tx_done(tx);
-		kefalnd_send_completion(efa_ni, conn, EFALND_MSG_PUTR_DONE, rc, cookie);
+		kefalnd_send_completion(efa_ni, conn, EFALND_MSG_PUTR_DONE, rc, src_cookie);
 		return rc;
 	}
 
-	kefalnd_init_rdma_read(conn, tx, EFALND_MSG_PUTR_DONE, &rx->msg->u.putr_req.rdma_desc,
-			       cookie);
+	kefalnd_init_tx_rdma_read(conn, tx, EFALND_MSG_PUTR_DONE, src_rd, src_cookie);
 
 	if (nob == 0) {
 		/* No RDMA: local completion may happen now! */
@@ -1094,18 +1036,33 @@ kefalnd_handle_putr_req(struct kefa_ni *efa_ni, struct kefa_conn *conn, struct k
 	return 0;
 }
 
+static inline int
+kefalnd_handle_putr_req_v1(struct kefa_ni *efa_ni, struct kefa_conn *conn, struct lnet_msg *lntmsg,
+			   struct kefa_putr_req_msg *putr_req, int nob)
+{
+	return kefalnd_handle_putr_req(efa_ni, conn, lntmsg, &putr_req->rdma_desc,
+				       putr_req->cookie, nob);
+}
+
+static inline int
+kefalnd_handle_putr_req_v2(struct kefa_ni *efa_ni, struct kefa_conn *conn, struct lnet_msg *lntmsg,
+			   struct kefa_putr_req_msg_v2 *putr_req, int nob)
+{
+	return kefalnd_handle_putr_req(efa_ni, conn, lntmsg, &putr_req->rdma_desc,
+				       putr_req->cookie, nob);
+}
+
 static int
-kefalnd_handle_getr_req(struct kefa_ni *efa_ni, struct kefa_conn *conn, struct kefa_rx *rx,
-			struct lnet_msg *lntmsg)
+kefalnd_handle_getr_req(struct kefa_ni *efa_ni, struct kefa_conn *conn, struct lnet_msg *lntmsg,
+			u64 sink_cookie)
 {
-	u64 cookie = rx->msg->u.getr_req.sink_cookie;
 	struct kefa_rdma_desc *rd;
 	struct kefa_tx *tx;
 	unsigned int nob;
 	int rc = 0;
 
 	if (lntmsg == NULL) {
-		kefalnd_send_completion(efa_ni, conn, EFALND_MSG_NACK, -ENODATA, cookie);
+		kefalnd_send_completion(efa_ni, conn, EFALND_MSG_NACK, -ENODATA, sink_cookie);
 		return 0;
 	}
 
@@ -1113,24 +1070,24 @@ kefalnd_handle_getr_req(struct kefa_ni *efa_ni, struct kefa_conn *conn, struct k
 
 	tx = kefalnd_get_idle_tx(efa_ni);
 	if (tx == NULL) {
-		EFA_DEV_ERR(efa_ni->efa_dev, "can't get tx for %s to peer NI[%s]\n",
-			    kefalnd_msgtype2str(rx->msg->type), libcfs_nidstr(&conn->remote_nid));
+		EFA_DEV_ERR(efa_ni->efa_dev, "can't allocate %s TX to peer NI[%s]\n",
+			    kefalnd_msgtype2str(EFALND_MSG_GETR_ACK),
+			    libcfs_nidstr(&conn->remote_nid));
 
 		return -ENOMEM;
 	}
 
-	rd = &tx->msg->u.getr_ack.rdma_desc;
-	tx->dmadir = DMA_TO_DEVICE;
+	rd = &tx->msg->msg_v1.u.getr_ack.rdma_desc;
 	if (nob != 0)
-		rc = kefalnd_map_msg_iov(efa_ni, tx, rd, lntmsg->msg_niov,
-					 lntmsg->msg_kiov, lntmsg->msg_offset, nob);
+		rc = kefalnd_map_msg_iov(efa_ni, tx, rd, lntmsg->msg_niov, lntmsg->msg_kiov,
+					 lntmsg->msg_offset, nob, true);
 	if (rc != 0) {
 		EFA_DEV_ERR(efa_ni->efa_dev, "can't setup GET src for peer NI[%s]. err[%d]\n",
 			    libcfs_nidstr(&conn->remote_nid), rc);
 
 		tx->hstatus = LNET_MSG_STATUS_LOCAL_ERROR;
 		kefalnd_tx_done(tx);
-		kefalnd_send_completion(efa_ni, conn, EFALND_MSG_NACK, rc, cookie);
+		kefalnd_send_completion(efa_ni, conn, EFALND_MSG_NACK, rc, sink_cookie);
 		return rc;
 	}
 
@@ -1142,17 +1099,31 @@ kefalnd_handle_getr_req(struct kefa_ni *efa_ni, struct kefa_conn *conn, struct k
 		tx->lntmsg[0] = lntmsg;
 	}
 
+	tx->msg->msg_v1.u.getr_ack.sink_cookie = sink_cookie;
+	tx->msg->msg_v1.u.getr_ack.src_cookie = kefalnd_tx_to_idx(tx);
+	kefalnd_init_tx_protocol_msg(tx, EFALND_MSG_GETR_ACK, sizeof(struct kefa_getr_ack_msg));
+
 	atomic_inc(&tx->ref_cnt); /* Wait for GETR_DONE */
 	atomic_set(&tx->waiting_resp, true);
 
-	tx->msg->u.getr_ack.sink_cookie = cookie;
-	tx->msg->u.getr_ack.src_cookie = kefalnd_tx_to_idx(tx);
-	kefalnd_init_tx_msg(tx, EFALND_MSG_GETR_ACK, sizeof(struct kefa_getr_ack_msg));
-
 	kefalnd_launch_tx(conn, tx);
 	return 0;
 }
 
+static inline int
+kefalnd_handle_getr_req_v1(struct kefa_ni *efa_ni, struct kefa_conn *conn, struct lnet_msg *lntmsg,
+			   struct kefa_getr_req_msg *getr_req)
+{
+	return kefalnd_handle_getr_req(efa_ni, conn, lntmsg, getr_req->sink_cookie);
+}
+
+static inline int
+kefalnd_handle_getr_req_v2(struct kefa_ni *efa_ni, struct kefa_conn *conn, struct lnet_msg *lntmsg,
+			   struct kefa_getr_req_msg_v2 *getr_req)
+{
+	return kefalnd_handle_getr_req(efa_ni, conn, lntmsg, getr_req->sink_cookie);
+}
+
 static int
 kefalnd_refill_rx(struct kefa_qp *qp, u32 budget)
 {
@@ -1189,7 +1160,6 @@ kefalnd_refill_rx(struct kefa_qp *qp, u32 budget)
 		budget--;
 	}
 
-
 	if (unlikely(wr_cnt == 0)) {
 		spin_unlock_irqrestore(&qp->rq_lock, flags);
 		return 0;
@@ -1207,12 +1177,6 @@ kefalnd_refill_rx(struct kefa_qp *qp, u32 budget)
 	qp->rq_space -= wr_cnt;
 
 	spin_unlock_irqrestore(&qp->rq_lock, flags);
-
-	/* TT: https://issues.amazon.com/issues/EFA-3459 */
-	rc = ib_req_notify_cq(qp->cq->ib_cq, IB_CQ_NEXT_COMP);
-	if (rc < 0)
-		EFA_DEV_ERR(qp->cq->efa_dev, "request notify CQ failed. err[%d]\n", rc);
-
 	return rc;
 }
 
@@ -1230,7 +1194,6 @@ kefalnd_free_rx(struct kefa_rx *rx)
 	return 0;
 }
 
-
 static int
 kefalnd_recv(struct lnet_ni *ni, void *private, struct lnet_msg *lntmsg,
 	    int delayed, unsigned int niov, struct bio_vec *kiov,
@@ -1238,49 +1201,68 @@ kefalnd_recv(struct lnet_ni *ni, void *private, struct lnet_msg *lntmsg,
 {
 	struct kefa_ni *efa_ni = ni->ni_data;
 	struct kefa_rx *rx = private;
-	struct kefa_msg *rxmsg = rx->msg;
+	unsigned int imm_offset;
 	struct kefa_conn *conn;
-	int nob, rc = 0;
+	struct kefa_msg *msg;
+	struct lnet_nid nid;
+	int imm_nob, rc = 0;
 
-	conn = kefalnd_get_conn_from_rx(efa_ni, rx, KEFA_CONN_TYPE_RESPONDER);
+	msg = rx->msg;
+	kefalnd_get_srcnid_from_msg(msg, &nid);
+	conn = kefalnd_lookup_conn(efa_ni, &nid, KEFA_CONN_TYPE_RESPONDER);
 	if (IS_ERR_OR_NULL(conn)) {
 		EFA_DEV_ERR(efa_ni->efa_dev,
 			    "failed to get connection for RX from peer NI[%s]. err[%ld]\n",
-			    libcfs_nid2str(rx->msg->srcnid), PTR_ERR(conn));
+			    libcfs_nidstr(&nid), PTR_ERR(conn));
 
 		return -ENOTCONN;
 	}
 
-	switch (rxmsg->type) {
+	switch (msg->hdr.type) {
 	default:
 		EFA_DEV_ERR(efa_ni->efa_dev, "received unexpected efa msg[%s] from peer NI[%s]\n",
-			    kefalnd_msgtype2str(rxmsg->type),
-			    libcfs_nidstr(&lntmsg->msg_hdr.src_nid));
+			    kefalnd_msgtype2str(msg->hdr.type), libcfs_nidstr(&nid));
 
 		break;
 
 	case EFALND_MSG_IMMEDIATE:
-		nob = offsetof(struct kefa_msg, u.immediate.payload[rlen]);
-		if (nob > rx->rx_nob) {
+		if (msg->hdr.proto_ver != EFALND_PROTO_VER_1) {
+			imm_offset = offsetof(struct kefa_msg, msg_v2.u.immediate.payload);
+			imm_nob = offsetof(struct kefa_msg, msg_v2.u.immediate.payload[rlen]);
+		} else {
+			imm_offset = offsetof(struct kefa_msg, msg_v1.u.immediate.payload);
+			imm_nob = offsetof(struct kefa_msg, msg_v1.u.immediate.payload[rlen]);
+		}
+
+		if (imm_nob > rx->rx_nob) {
 			EFA_DEV_ERR(efa_ni->efa_dev,
 				    "immediate message from peer NI[%s] too big: %d(%d)\n",
-				    libcfs_nidstr(&lntmsg->msg_hdr.src_nid), nob, rx->rx_nob);
+				    libcfs_nidstr(&nid), imm_nob, rx->rx_nob);
 
 			rc = -EPROTO;
 			break;
 		}
 
-		lnet_copy_flat2kiov(niov, kiov, offset, EFALND_MSG_SIZE, rxmsg,
-				    offsetof(struct kefa_msg, u.immediate.payload), mlen);
+		lnet_copy_flat2kiov(niov, kiov, offset, EFALND_MSG_SIZE, msg, imm_offset, mlen);
 		lnet_finalize(lntmsg, 0);
 		break;
 
 	case EFALND_MSG_PUTR_REQ:
-		rc = kefalnd_handle_putr_req(efa_ni, conn, rx, lntmsg, mlen);
+		if (msg->hdr.proto_ver != EFALND_PROTO_VER_1)
+			rc = kefalnd_handle_putr_req_v2(efa_ni, conn, lntmsg,
+							&msg->msg_v2.u.putr_req, mlen);
+		else
+			rc = kefalnd_handle_putr_req_v1(efa_ni, conn, lntmsg,
+							&msg->msg_v1.u.putr_req, mlen);
 		break;
 
 	case EFALND_MSG_GETR_REQ:
-		rc = kefalnd_handle_getr_req(efa_ni, conn, rx, lntmsg);
+		if (msg->hdr.proto_ver != EFALND_PROTO_VER_1)
+			rc = kefalnd_handle_getr_req_v2(efa_ni, conn, lntmsg,
+							&msg->msg_v2.u.getr_req);
+		else
+			rc = kefalnd_handle_getr_req_v1(efa_ni, conn, lntmsg,
+							&msg->msg_v1.u.getr_req);
 		break;
 	}
 
@@ -1289,9 +1271,10 @@ kefalnd_recv(struct lnet_ni *ni, void *private, struct lnet_msg *lntmsg,
 }
 
 static void
-kefalnd_finv_complete(struct kefa_fmr *fmr, struct ib_wc *wc)
+kefalnd_finv_complete(struct kefa_ni *efa_ni, struct ib_wc *wc)
 {
-	struct kefa_qp *qp;
+	struct kefa_dev *efa_dev = efa_ni->efa_dev;
+	struct kefa_fmr *fmr = (void *)wc->wr_id;
 
 	if (!fmr) {
 		/* Reaching here means FW or LND did something bad */
@@ -1300,26 +1283,26 @@ kefalnd_finv_complete(struct kefa_fmr *fmr, struct ib_wc *wc)
 		return;
 	}
 
-	qp = fmr->inv_qp;
 	if (wc->status != IB_WC_SUCCESS) {
-		EFA_DEV_WARN(qp->efa_dev,
+		EFA_DEV_WARN(efa_dev,
 			     "QP[%u] received FINV[0x%x] completion with err[%u] vendor[%u]\n",
-			     qp->ib_qp->qp_num, fmr->mr->lkey, wc->status, wc->vendor_err);
+			     wc->qp->qp_num, fmr->mr->lkey, wc->status, wc->vendor_err);
 
-		kefalnd_obj_pool_put_on_pend_list(&qp->efa_dev->fmr_pool, &fmr->list_node);
+		kefalnd_obj_pool_put_on_pend_list(&efa_dev->fmr_pool, &fmr->list_node);
 		return;
 	}
 
 	fmr->state = KEFA_FMR_INACTIVE;
-	kefalnd_obj_pool_free(&qp->efa_dev->fmr_pool, &fmr->list_node);
+	kefalnd_obj_pool_free(&efa_dev->fmr_pool, &fmr->list_node);
 }
 
 static void
-kefalnd_tx_complete(struct kefa_tx *tx, struct ib_wc *wc)
+kefalnd_tx_complete(struct kefa_ni *efa_ni, struct ib_wc *wc)
 {
 	struct kefa_conn *conn;
-	struct kefa_qp *qp;
+	struct kefa_tx *tx;
 
+	tx = (void *)wc->wr_id;
 	if (!tx) {
 		/* Reaching here means FW or LND did something bad */
 		CERROR("cpu[%u] received bad TX completion with status[%u]",
@@ -1328,17 +1311,15 @@ kefalnd_tx_complete(struct kefa_tx *tx, struct ib_wc *wc)
 	}
 
 	conn = tx->conn;
-	qp = tx->qp;
-
-	if (!conn || !qp) {
-		CERROR("tx[%p] received bad conn[%p] or qp[%p] type[%s]", tx, conn, qp,
+	if (!conn) {
+		CERROR("TX[%p] received bad conn[%p], type[%s]", tx, conn,
 		       kefalnd_msgtype2str(tx->type));
 
 		return;
 	}
 
 	if (atomic_read(&tx->ref_cnt) <= 0) {
-		EFA_DEV_ERR(qp->efa_dev, "received completion on free tx\n");
+		EFA_DEV_ERR(efa_ni->efa_dev, "received completion on free TX\n");
 		return;
 	}
 
@@ -1347,17 +1328,16 @@ kefalnd_tx_complete(struct kefa_tx *tx, struct ib_wc *wc)
 			tx->fmr->state = KEFA_FMR_ACTIVE;
 			kefalnd_launch_tx(tx->conn, tx);
 		} else {
-			EFA_DEV_ERR(qp->efa_dev,
+			EFA_DEV_ERR(efa_ni->efa_dev,
 				    "QP[%u] received FRWR[0x%x] completion with err[%u] vendor[%u]\n",
-				     qp->ib_qp->qp_num, tx->fmr->mr->lkey, wc->status,
-				     wc->vendor_err);
+				     wc->qp->qp_num, tx->fmr->mr->lkey, wc->status, wc->vendor_err);
 
 			kefalnd_abort_tx(tx, LNET_MSG_STATUS_LOCAL_DROPPED, -ECOMM);
 		}
 	} else if (wc->status != IB_WC_SUCCESS) {
-		EFA_DEV_ERR(qp->efa_dev,
+		EFA_DEV_ERR(efa_ni->efa_dev,
 			    "QP[%u] received TX[%s] completion with err. opcode[%u] status[%u] vendor[%u] peer_ni[%s]\n",
-			    qp->ib_qp->qp_num, kefalnd_msgtype2str(tx->type), wc->opcode,
+			    wc->qp->qp_num, kefalnd_msgtype2str(tx->type), wc->opcode,
 			    wc->status, wc->vendor_err, libcfs_nidstr(&conn->remote_nid));
 
 		kefalnd_abort_tx(tx, LNET_MSG_STATUS_REMOTE_DROPPED, -ECOMM);
@@ -1370,10 +1350,10 @@ kefalnd_tx_complete(struct kefa_tx *tx, struct ib_wc *wc)
 }
 
 static void
-kefalnd_handle_completion(struct kefa_ni *efa_ni, struct kefa_msg *msg)
+kefalnd_handle_completion(struct kefa_ni *efa_ni, struct kefa_completion_msg *completion)
 {
-	int status = kefalnd_efa_status_to_errno(msg->u.completion.status);
-	u64 tx_idx = msg->u.completion.cookie;
+	int status = kefalnd_efa_status_to_errno(completion->status);
+	u64 tx_idx = completion->cookie;
 	struct kefa_tx *tx;
 
 	tx = kefalnd_get_tx_by_idx(efa_ni, tx_idx);
@@ -1396,16 +1376,12 @@ kefalnd_handle_completion(struct kefa_ni *efa_ni, struct kefa_msg *msg)
 }
 
 static int
-kefalnd_handle_getr(struct kefa_ni *efa_ni, struct kefa_rx *rx)
+kefalnd_handle_getr_ack(struct kefa_ni *efa_ni, struct kefa_getr_ack_msg *getr_ack)
 {
-	struct kefa_msg *msg = rx->msg;
 	struct kefa_conn *conn;
-	u64 tx_idx, src_cookie;
 	struct kefa_tx *tx;
-	int nob;
 
-	tx_idx = msg->u.getr_ack.sink_cookie;
-	tx = kefalnd_get_tx_by_idx(efa_ni, tx_idx);
+	tx = kefalnd_get_tx_by_idx(efa_ni, getr_ack->sink_cookie);
 	if (!tx)
 		return -EINVAL;
 
@@ -1413,16 +1389,13 @@ kefalnd_handle_getr(struct kefa_ni *efa_ni, struct kefa_rx *rx)
 	if (!atomic_xchg_relaxed(&tx->waiting_resp, false))
 		return -EINVAL;
 
-	nob =  kefalnd_rdma_frag_size(&msg->u.getr_ack.rdma_desc);
 	conn = tx->conn;
 
-	lnet_set_reply_msg_len(efa_ni->lnet_ni, tx->lntmsg[1], nob);
-
-	src_cookie = msg->u.getr_ack.src_cookie;
+	lnet_set_reply_msg_len(efa_ni->lnet_ni, tx->lntmsg[1], getr_ack->rdma_desc.nob);
 
 	/* source has mapped his buffers - let's read */
-	kefalnd_init_rdma_read(conn, tx, EFALND_MSG_GETR_DONE, &msg->u.getr_ack.rdma_desc,
-			       src_cookie);
+	kefalnd_init_tx_rdma_read(conn, tx, EFALND_MSG_GETR_DONE,
+				  &getr_ack->rdma_desc, getr_ack->src_cookie);
 
 	kefalnd_launch_tx(conn, tx);
 
@@ -1437,77 +1410,105 @@ kefalnd_handle_getr(struct kefa_ni *efa_ni, struct kefa_rx *rx)
 }
 
 static void
-kefalnd_handle_rx(struct kefa_ni *efa_ni, struct kefa_rx *rx)
+kefalnd_handle_lnet_request_msg(struct kefa_ni *efa_ni, struct kefa_rx *rx)
 {
 	struct kefa_msg *msg = rx->msg;
+	int rc, rdma_req = 0;
 	struct lnet_hdr hdr;
-	bool free_rx = false;
-	int rc = 0;
+	bool proto_v1;
 
-	switch (msg->type) {
-	default:
-		EFA_DEV_ERR(efa_ni->efa_dev, "bad EFALND message type %x from loopback\n",
-			    msg->type);
+	proto_v1 = msg->hdr.proto_ver == EFALND_PROTO_VER_1;
 
-		rc = -EPROTO;
+	switch (msg->hdr.type) {
+	case EFALND_MSG_IMMEDIATE:
+		if (proto_v1)
+			lnet_hdr_from_nid4(&hdr, &msg->msg_v1.u.immediate.hdr);
+		else
+			lnet_hdr_from_nid16(&hdr, &msg->msg_v2.u.immediate.hdr);
 		break;
 
-	case EFALND_MSG_IMMEDIATE:
-		lnet_hdr_from_nid4(&hdr, &msg->u.immediate.hdr);
-		rc = lnet_parse(efa_ni->lnet_ni, &hdr, &hdr.src_nid, rx, 0);
-		if (rc < 0)
-			EFA_DEV_ERR(efa_ni->efa_dev, "error parsing lnet msg\n");
+	case EFALND_MSG_PUTR_REQ:
+		rdma_req = 1;
+		if (proto_v1)
+			lnet_hdr_from_nid4(&hdr, &msg->msg_v1.u.putr_req.hdr);
+		else
+			lnet_hdr_from_nid16(&hdr, &msg->msg_v2.u.putr_req.hdr);
+		break;
+
+	case EFALND_MSG_GETR_REQ:
+		rdma_req = 1;
+		if (proto_v1)
+			lnet_hdr_from_nid4(&hdr, &msg->msg_v1.u.getr_req.hdr);
+		else
+			lnet_hdr_from_nid16(&hdr, &msg->msg_v2.u.getr_req.hdr);
+		break;
 
+	default:
+		LASSERTF(0, "message type[%u] doesn't have lnet header\n", msg->hdr.type);
 		break;
+	}
 
-	case EFALND_MSG_PUTR_REQ:
-		lnet_hdr_from_nid4(&hdr, &msg->u.putr_req.hdr);
-		rc = lnet_parse(efa_ni->lnet_ni, &hdr, &hdr.src_nid, rx, 0);
-		if (rc < 0)
-			EFA_DEV_ERR(efa_ni->efa_dev, "error parsing lnet msg\n");
+	rc = lnet_parse(efa_ni->lnet_ni, &hdr, &hdr.src_nid, rx, rdma_req);
+	if (rc < 0)
+		EFA_DEV_ERR(efa_ni->efa_dev, "error parsing lnet msg\n");
+}
+
+static inline struct kefa_getr_ack_msg *
+kefalnd_get_getr_ack_from_msg(struct kefa_msg *msg)
+{
+	if (msg->hdr.proto_ver != EFALND_PROTO_VER_1)
+		return &msg->msg_v2.u.getr_ack;
 
+	return &msg->msg_v1.u.getr_ack;
+}
+
+static inline struct kefa_completion_msg *
+kefalnd_get_completion_from_msg(struct kefa_msg *msg)
+{
+	if (msg->hdr.proto_ver != EFALND_PROTO_VER_1)
+		return &msg->msg_v2.u.completion;
+
+	return &msg->msg_v1.u.completion;
+}
+
+static void
+kefalnd_handle_rx(struct kefa_ni *efa_ni, struct kefa_rx *rx)
+{
+	struct kefa_msg *msg = rx->msg;
+	bool free_rx = false;
+	int rc = 0;
+
+	switch (msg->hdr.type) {
+	default:
+		EFA_DEV_ERR(efa_ni->efa_dev, "bad EFALND message type %x from loopback\n",
+			    msg->hdr.type);
+
+		rc = -EPROTO;
 		break;
 
 	case EFALND_MSG_GETR_REQ:
-		lnet_hdr_from_nid4(&hdr, &msg->u.getr_req.hdr);
-		rc = lnet_parse(efa_ni->lnet_ni, &hdr, &hdr.src_nid, rx, 1);
-		if (rc < 0)
-			EFA_DEV_ERR(efa_ni->efa_dev, "error parsing lnet msg\n");
-
+	case EFALND_MSG_PUTR_REQ:
+	case EFALND_MSG_IMMEDIATE:
+		kefalnd_handle_lnet_request_msg(efa_ni, rx);
 		break;
 
 	case EFALND_MSG_GETR_ACK:
-		kefalnd_handle_getr(efa_ni, rx);
+		kefalnd_handle_getr_ack(efa_ni, kefalnd_get_getr_ack_from_msg(msg));
 		free_rx = true;
 		break;
 
 	case EFALND_MSG_NACK:
-		CDEBUG(D_NET, "Received NACK from[%s] to[%s]\n",
-		       libcfs_nid2str(msg->srcnid), libcfs_nid2str(msg->dstnid));
-		fallthrough;
 	case EFALND_MSG_PUTR_DONE:
 	case EFALND_MSG_GETR_DONE:
-		kefalnd_handle_completion(efa_ni, msg);
+		kefalnd_handle_completion(efa_ni, kefalnd_get_completion_from_msg(msg));
 		free_rx = true;
 		break;
 
 	case EFALND_MSG_CONN_PROBE:
-		kefalnd_handle_conn_probe(efa_ni, rx);
-		free_rx = true;
-		break;
-
 	case EFALND_MSG_CONN_PROBE_RESP:
-		kefalnd_handle_conn_probe_resp(efa_ni, rx);
-		free_rx = true;
-		break;
-
 	case EFALND_MSG_CONN_REQ:
-		kefalnd_handle_conn_req(efa_ni, rx);
-		free_rx = true;
-		break;
-
 	case EFALND_MSG_CONN_REQ_ACK:
-		kefalnd_handle_conn_req_ack(efa_ni, rx);
+		kefalnd_handle_conn_establishment(efa_ni, msg);
 		free_rx = true;
 		break;
 	}
@@ -1519,112 +1520,131 @@ kefalnd_handle_rx(struct kefa_ni *efa_ni, struct kefa_rx *rx)
 }
 
 static int
-kefalnd_unpack_msg(struct kefa_dev *efa_dev, struct kefa_msg *msg, int nob)
+kefalnd_unpack_header_v2(struct kefa_ni *efa_ni, struct kefa_msg_v2 *msg_v2, u8 type, int rx_nob)
 {
-	const int min_hdr_size = offsetof(struct kefa_msg, type);
-	const int base_hdr_size = offsetof(struct kefa_msg, u);
-	int msg_nob;
+	const int base_hdr_size = offsetof(struct kefa_msg, msg_v2.u);
+	struct kefa_dev *efa_dev = efa_ni->efa_dev;
 
-	/* min_hdr_size required to read magic + protocol version */
-	if (nob < min_hdr_size) {
-		EFA_DEV_ERR(efa_dev, "short message: %d\n", nob);
-		goto bad_pkt;
+	if (rx_nob < base_hdr_size) {
+		EFA_DEV_ERR(efa_dev, "short message: %d\n", rx_nob);
+		return -EPROTO;
 	}
 
-	if (msg->magic != EFALND_MSG_MAGIC) {
-		EFA_DEV_ERR(efa_dev, "bad magic: %08x\n", msg->magic);
-		goto bad_pkt;
+	if (type != EFALND_MSG_CONN_PROBE && msg_v2->dst_epoch != efa_ni->ni_epoch) {
+		EFA_DEV_ERR(efa_dev, "RX[%u] epoch mismatch: recv[%llu], expected[%llu]\n",
+			    type, msg_v2->dst_epoch, efa_ni->ni_epoch);
+		return -EPROTO;
 	}
 
-	if (msg->type != EFALND_MSG_CONN_PROBE && msg->proto_ver > EFALND_CURR_PROTO_VER) {
-		EFA_DEV_ERR(efa_dev, "bad protocol version: %x\n", msg->proto_ver);
-		goto bad_pkt;
+	if (LNET_NID_IS_ANY(&msg_v2->srcnid)) {
+		EFA_DEV_ERR(efa_dev, "bad src nid: %s\n", libcfs_nidstr(&msg_v2->srcnid));
+		return -EPROTO;
 	}
 
-	if (nob < base_hdr_size) {
-		EFA_DEV_ERR(efa_dev, "short message: %d\n", nob);
-		goto bad_pkt;
-	}
+	return 0;
+}
 
-	msg_nob = msg->nob;
-	if (msg_nob > nob) {
-		EFA_DEV_ERR(efa_dev, "short message: got %d, wanted %d\n", nob, msg_nob);
-		goto bad_pkt;
+static int
+kefalnd_unpack_header_v1(struct kefa_ni *efa_ni, struct kefa_msg_v1 *msg_v1, u8 type, int rx_nob)
+{
+	const int base_hdr_size = offsetof(struct kefa_msg, msg_v1.u);
+	struct kefa_dev *efa_dev = efa_ni->efa_dev;
+
+	if (rx_nob < base_hdr_size) {
+		EFA_DEV_ERR(efa_dev, "short message: %d\n", rx_nob);
+		return -EPROTO;
 	}
 
-	if (msg->type != EFALND_MSG_CONN_PROBE && msg->dst_epoch != efa_dev->efa_ni->ni_epoch) {
+	if (type != EFALND_MSG_CONN_PROBE && msg_v1->dst_epoch != efa_ni->ni_epoch) {
 		EFA_DEV_ERR(efa_dev, "RX[%u] epoch mismatch: recv[%llu], expected[%llu]\n",
-			    msg->type, msg->dst_epoch, efa_dev->efa_ni->ni_epoch);
-		goto bad_pkt;
+			    type, msg_v1->dst_epoch, efa_ni->ni_epoch);
+		return -EPROTO;
 	}
 
-	if (msg->srcnid == LNET_NID_ANY) {
-		EFA_DEV_ERR(efa_dev, "bad src nid: %s\n", libcfs_nid2str(msg->srcnid));
-		goto bad_pkt;
+	if (msg_v1->srcnid == LNET_NID_ANY) {
+		EFA_DEV_ERR(efa_dev, "bad src nid: %s\n", libcfs_nid2str(msg_v1->srcnid));
+		return -EPROTO;
 	}
 
-	if (msg_nob < kefalnd_msgtype2size(msg->type)) {
-		EFA_DEV_ERR(efa_dev, "short %s: %d(%d)\n", kefalnd_msgtype2str(msg->type),
-			    msg_nob, kefalnd_msgtype2size(msg->type));
+	return 0;
+}
 
-		goto bad_pkt;
+static int
+kefalnd_unpack_msg(struct kefa_ni *efa_ni, struct kefa_rx *rx, struct ib_wc *wc)
+{
+	const int min_hdr_size = sizeof(struct kefa_hdr);
+	struct kefa_msg *msg = rx->msg;
+	struct kefa_hdr *hdr;
+	struct lnet_nid nid;
+	int rc = 0;
+
+	hdr = &msg->hdr;
+	if (rx->rx_nob < min_hdr_size) {
+		EFA_DEV_ERR(efa_ni->efa_dev, "short message: %d\n", rx->rx_nob);
+		return -EPROTO;
 	}
 
-	switch (msg->type) {
-	default:
-		EFA_DEV_ERR(efa_dev, "unknown message type %x\n", msg->type);
-		goto bad_pkt;
+	if (hdr->magic != EFALND_MSG_MAGIC) {
+		EFA_DEV_ERR(efa_ni->efa_dev, "bad magic: %08x\n", hdr->magic);
+		return -EPROTO;
+	}
 
-	case EFALND_MSG_CONN_PROBE:
-	case EFALND_MSG_CONN_PROBE_RESP:
-	case EFALND_MSG_CONN_REQ:
-	case EFALND_MSG_CONN_REQ_ACK:
-		break;
+	if (hdr->proto_ver > EFALND_MAX_PROTO_VER ||
+	    (hdr->type != EFALND_MSG_CONN_PROBE && hdr->proto_ver < EFALND_MIN_PROTO_VER)) {
+		EFA_DEV_ERR(efa_ni->efa_dev, "bad protocol version: %x\n", hdr->proto_ver);
+		return -EPROTO;
+	}
 
-	case EFALND_MSG_IMMEDIATE:
-	case EFALND_MSG_PUTR_REQ:
-	case EFALND_MSG_PUTR_DONE:
-	case EFALND_MSG_GETR_REQ:
-	case EFALND_MSG_GETR_ACK:
-	case EFALND_MSG_GETR_DONE:
-	case EFALND_MSG_NACK:
-		break;
+	if (hdr->nob > rx->rx_nob) {
+		EFA_DEV_ERR(efa_ni->efa_dev, "short message: got %d, wanted %d\n", rx->rx_nob,
+			    hdr->nob);
+
+		return -EPROTO;
 	}
+
+	if (hdr->nob < kefalnd_msgtype2size(hdr->type)) {
+		EFA_DEV_ERR(efa_ni->efa_dev, "short %s: %d(%d)\n", kefalnd_msgtype2str(hdr->type),
+			    hdr->nob, kefalnd_msgtype2size(hdr->type));
+
+		return -EPROTO;
+	}
+
+	if (hdr->proto_ver != EFALND_PROTO_VER_1)
+		rc = kefalnd_unpack_header_v2(efa_ni, &msg->msg_v2, hdr->type, rx->rx_nob);
+	else
+		rc = kefalnd_unpack_header_v1(efa_ni, &msg->msg_v1, hdr->type, rx->rx_nob);
+
+	if (rc)
+		goto bad_pkt;
+
 	return 0;
 
 bad_pkt:
-	kefalnd_hexdump_buf("rx-buf: ", msg, sizeof(*msg));
+	kefalnd_get_srcnid_from_msg(msg, &nid);
+	EFA_DEV_ERR(efa_ni->efa_dev, "QP[%u] failed RX unpacking from peer NI[%s].\n",
+		    wc->qp->qp_num, libcfs_nidstr(&nid));
+
 	return -EPROTO;
 }
 
 static void
-kefalnd_rx_complete(struct kefa_rx *rx, int status, int nob, int slid)
+kefalnd_rx_complete(struct kefa_ni *efa_ni, struct ib_wc *wc)
 {
-	struct kefa_dev *efa_dev;
-	struct kefa_ni *efa_ni;
-	struct kefa_msg *msg;
-	struct kefa_qp *qp;
-	int rc;
+	struct kefa_rx *rx;
+	int nob, rc;
 
+	rx = (void *)wc->wr_id;
+	nob = wc->byte_len;
 	if (!rx || nob == 0) {
 		/* Reaching here means FW or LND did something bad */
 		CERROR("cpu[%u] received bad RX handle with status[%u] nob[%u]",
-		       smp_processor_id(), status, nob);
+		       smp_processor_id(), wc->status, nob);
 		return;
 	}
 
-	qp = rx->qp;
-	msg = rx->msg;
-	efa_dev = qp->efa_dev;
-	efa_ni = efa_dev->efa_ni;
-
-	CDEBUG(D_NET, "Received RX type[%s] from[%s] to[%s]\n", kefalnd_msgtype2str(rx->msg->type),
-	       libcfs_nid2str(msg->srcnid), libcfs_nid2str(msg->dstnid));
-
-	if (status != IB_WC_SUCCESS) {
-		EFA_DEV_ERR(efa_dev,
-			    "QP[%u] received RX completion with err[%u] from peer NI[%s]\n",
-			    qp->ib_qp->qp_num, status, libcfs_nid2str(msg->srcnid));
+	if (wc->status != IB_WC_SUCCESS) {
+		EFA_DEV_ERR(efa_ni->efa_dev, "QP[%u] received RX completion with err[%u]\n",
+			    wc->qp->qp_num, wc->status);
 
 		goto failed;
 	}
@@ -1632,13 +1652,9 @@ kefalnd_rx_complete(struct kefa_rx *rx, int status, int nob, int slid)
 	LASSERT(rx->rx_nob < 0);	/* was posted */
 	rx->rx_nob = nob;
 
-	rc = kefalnd_unpack_msg(efa_dev, msg, rx->rx_nob);
-	if (rc != 0) {
-		EFA_DEV_ERR(efa_dev, "QP[%u] failed RX unpacking from peer NI[%s]. err[%d]\n",
-			    qp->ib_qp->qp_num, libcfs_nid2str(msg->srcnid), rc);
-
+	rc = kefalnd_unpack_msg(efa_ni, rx, wc);
+	if (rc)
 		goto failed;
-	}
 
 	kefalnd_handle_rx(efa_ni, rx);
 	return;
@@ -1648,12 +1664,8 @@ kefalnd_rx_complete(struct kefa_rx *rx, int status, int nob, int slid)
 }
 
 static void
-kefalnd_complete(struct ib_wc *wc)
+kefalnd_complete(struct kefa_ni *efa_ni, struct ib_wc *wc)
 {
-	struct kefa_fmr *fmr;
-	struct kefa_tx *tx;
-	struct kefa_rx *rx;
-
 	switch (wc->opcode) {
 	default:
 		LBUG();
@@ -1661,18 +1673,15 @@ kefalnd_complete(struct ib_wc *wc)
 	case IB_WC_SEND:
 	case IB_WC_REG_MR:
 	case IB_WC_RDMA_READ:
-		tx = (void *)wc->wr_id;
-		kefalnd_tx_complete(tx,  wc);
+		kefalnd_tx_complete(efa_ni, wc);
 		return;
 
 	case IB_WC_LOCAL_INV:
-		fmr = (void *)wc->wr_id;
-		kefalnd_finv_complete(fmr, wc);
+		kefalnd_finv_complete(efa_ni, wc);
 		return;
 
 	case IB_WC_RECV:
-		rx = (void *)wc->wr_id;
-		kefalnd_rx_complete(rx, wc->status, wc->byte_len, wc->slid);
+		kefalnd_rx_complete(efa_ni, wc);
 		return;
 	}
 }
@@ -1707,17 +1716,11 @@ kefalnd_scheduler(void *arg)
 		cq = list_first_entry_or_null(&sched->pend_cqs, struct kefa_cq, sched_node);
 		if (!cq) {
 			spin_unlock_irqrestore(&sched->lock, flags);
-			CDEBUG_LIMIT(D_NET, " ---- p[%u] no CQs to poll. sleeping ---\n",
-				     task_pid_nr(current));
-
 			set_current_state(TASK_INTERRUPTIBLE);
 			add_wait_queue_exclusive(&sched->waitq, &wait);
 			schedule();
 			remove_wait_queue(&sched->waitq, &wait);
 			set_current_state(TASK_RUNNING);
-			CDEBUG_LIMIT(D_NET, " ---- p[%u] back from sleep ---\n",
-				     task_pid_nr(current));
-
 			continue;
 		}
 		list_del_init(&cq->sched_node);
@@ -1755,7 +1758,7 @@ kefalnd_scheduler(void *arg)
 		spin_unlock_irqrestore(&sched->lock, flags);
 
 		for (i = 0; i < cqe_cnt; i++)
-			kefalnd_complete(wc + i);
+			kefalnd_complete(cq->efa_dev->efa_ni, wc + i);
 
 		/* respect periodic scheduling  */
 		if (need_resched())
@@ -2161,7 +2164,7 @@ kefalnd_destroy_qps(struct kefa_dev *efa_dev)
 
 /*  Create Data QPs to distribute traffic per device and manager QP for connection establishment */
 static int
-kefalnd_create_qps(struct kefa_dev *efa_dev, int num_qps)
+kefalnd_create_qps(struct kefa_dev *efa_dev, int num_qps, int sq_depth, int rq_depth)
 {
 	int i, rc = 0;
 	u32 qkey;
@@ -2176,7 +2179,7 @@ kefalnd_create_qps(struct kefa_dev *efa_dev, int num_qps)
 	}
 
 	rc = kefalnd_create_qp(efa_dev, efa_dev->cm_qp, efa_dev->cm_cq,
-			       SQ_DEPTH, RQ_DEPTH, EFALND_STATIC_QKEY);
+			       sq_depth, rq_depth, EFALND_STATIC_QKEY);
 	if (rc) {
 		kefalnd_destroy_qps(efa_dev);
 		return rc;
@@ -2196,7 +2199,7 @@ kefalnd_create_qps(struct kefa_dev *efa_dev, int num_qps)
 		qkey = get_random_u32() & ~BIT(31);
 		rc = kefalnd_create_qp(efa_dev, &efa_dev->qps[i],
 				       &efa_dev->cqs[i % efa_dev->ncqs],
-				       SQ_DEPTH, RQ_DEPTH, qkey);
+				       sq_depth, rq_depth, qkey);
 		if (rc)
 			break;
 	}
@@ -2244,9 +2247,7 @@ kefalnd_cq_comp_handler(struct ib_cq *ib_cq, void *cq_ctxt)
 	struct kefa_cq *cq = cq_ctxt;
 	struct kefa_sched *sched;
 
-	CDEBUG_LIMIT(D_NET, "CQ[0x%p] Received completion\n", cq);
 	sched = kefalnd_schedule_cq(cq);
-
 	wake_up(&sched->waitq);
 }
 
@@ -2314,7 +2315,7 @@ kefalnd_destroy_cqs(struct kefa_dev *efa_dev)
 }
 
 static int
-kefalnd_create_cqs(struct kefa_dev *efa_dev, int num_cqs)
+kefalnd_create_cqs(struct kefa_dev *efa_dev, int num_cqs, int cq_depth)
 {
 	int i, rc = 0;
 
@@ -2327,7 +2328,7 @@ kefalnd_create_cqs(struct kefa_dev *efa_dev, int num_cqs)
 		return -ENOMEM;
 	}
 
-	rc = kefalnd_create_cq(efa_dev, efa_dev->cm_cq, CQ_DEPTH);
+	rc = kefalnd_create_cq(efa_dev, efa_dev->cm_cq, cq_depth);
 	if (rc)
 		return rc;
 
@@ -2344,7 +2345,7 @@ kefalnd_create_cqs(struct kefa_dev *efa_dev, int num_cqs)
 	for (i = 0; i < num_cqs; i++) {
 		struct kefa_cq *cq = &efa_dev->cqs[i];
 
-		rc = kefalnd_create_cq(efa_dev, cq, CQ_DEPTH);
+		rc = kefalnd_create_cq(efa_dev, cq, cq_depth);
 		if (rc)
 			break;
 
@@ -2454,6 +2455,7 @@ kefalnd_dev_init(struct kefa_ni *efa_ni, char *ifname, uint32_t ip_addr)
 	struct lnet_ni *lnet_ni = efa_ni->lnet_ni;
 	struct kefa_dev *efa_dev = NULL;
 	bool took_module_ref = false;
+	int cq_depth, qp_depth;
 	int dev_numa_node, rc;
 
 	CDEBUG(D_NET, "Initializing EFA device\n");
@@ -2502,12 +2504,14 @@ kefalnd_dev_init(struct kefa_ni *efa_ni, char *ifname, uint32_t ip_addr)
 		goto failed;
 	}
 
+	cq_depth = min(efa_dev->ib_dev->attrs.max_cqe, CQ_DEPTH);
+	qp_depth = min(efa_dev->ib_dev->attrs.max_qp_wr, (cq_depth / 2));
 	tunables = &lnet_ni->ni_lnd_tunables.lnd_tun_u.lnd_efa;
-	rc = kefalnd_create_cqs(efa_dev, tunables->lnd_nqps);
+	rc = kefalnd_create_cqs(efa_dev, tunables->lnd_nqps, cq_depth);
 	if (rc)
 		goto failed;
 
-	rc = kefalnd_create_qps(efa_dev, tunables->lnd_nqps);
+	rc = kefalnd_create_qps(efa_dev, tunables->lnd_nqps, qp_depth, qp_depth);
 	if (rc)
 		goto failed;
 
@@ -2602,8 +2606,6 @@ kefalnd_base_shutdown(void)
 	cfs_percpt_free(kefalnd.scheds);
 	cfs_percpt_free(kefalnd.cm_daemons);
 
-	/* TODO - make sure no device is left on list */
-
 	CDEBUG(D_MALLOC, "After LND cleanup: kmem[%lld]\n",
 	       libcfs_kmem_read());
 
@@ -2855,7 +2857,7 @@ static void __exit kefalnd_exit(void)
 {
 	CDEBUG(D_NET, "Exiting EFA LND\n");
 
-	kefalnd_peerni_exit();
+	kefalnd_debugfs_exit();
 
 	lnet_unregister_lnd(&the_efalnd);
 }
@@ -2864,6 +2866,8 @@ static int __init kefalnd_init(void)
 {
 	int rc;
 
+	memset(&kefalnd, 0, sizeof(kefalnd));
+
 	CDEBUG(D_NET, "Entering EFA LND\n");
 	rc = kefalnd_tunables_init();
 	if (rc) {
@@ -2871,11 +2875,7 @@ static int __init kefalnd_init(void)
 		return rc;
 	}
 
-	rc = kefalnd_peerni_init();
-	if (rc) {
-		CERROR("failed to init peer_ni\n");
-		return rc;
-	}
+	kefalnd_debugfs_init();
 
 	lnet_register_lnd(&the_efalnd);
 	return 0;
diff --git a/net/lnet/lnet/klnds/efalnd/efalnd.h b/net/lnet/lnet/klnds/efalnd/efalnd.h
--- a/net/lnet/lnet/klnds/efalnd/efalnd.h
+++ b/net/lnet/lnet/klnds/efalnd/efalnd.h
@@ -1,7 +1,7 @@
 // SPDX-License-Identifier: GPL-2.0
 
 /*
- * Copyright (c) 2023-2024, Amazon and/or its affiliates. All rights reserved.
+ * Copyright (c) 2023-2025, Amazon and/or its affiliates. All rights reserved.
  * Use is subject to license terms.
  */
 
@@ -28,11 +28,11 @@
 #else
 #include <efa_verbs.h>
 #endif
-#include "efalnd-proto.h"
+#include "efalnd_proto.h"
 
 #define EFALND_MAJOR_VER        1
-#define EFALND_MINOR_VER        0
-#define EFALND_SUBMINOR_VER     9
+#define EFALND_MINOR_VER        1
+#define EFALND_SUBMINOR_VER     1
 #define EFALND_MAJOR_SHIFT	8
 
 #define KEFA_IFNAME_SIZE		256
@@ -56,7 +56,7 @@
 #define EFALND_MAX_SCHED_THRS		4
 
  /* TODO - use dynamic qkey for better security */
-#define EFALND_STATIC_QKEY	(0x1111)
+#define EFALND_STATIC_QKEY		(0x1111)
 
 /* TODO - use a tunable */
 #define EFALND_NO_RDMA_THRESH		EFALND_MSG_SIZE
@@ -67,9 +67,13 @@
 
 #define EFALND_MAX_PEER_QPS		256
 
+#define EFALND_MIN_INIT_CONN_TIMEOUT	200
+
 /* Define EFALND_CD so that we can easily add D_CONSOLE in test envs */
 #define EFALND_CD (D_NET)
 
+#define EFALND_FIELD_AVAIL(type, fld, sz) (offsetof(type, fld) < (sz))
+
 #define EFA_DEV_DEBUG(dev, format, ...) CDEBUG(EFALND_CD, "Device[%s] " format, (dev)->ifname, ## __VA_ARGS__)
 #define EFA_DEV_ERR(dev, format, ...) CERROR("Device[%s] " format, (dev)->ifname, ## __VA_ARGS__)
 #define EFA_DEV_WARN(dev, format, ...) CWARN("Device[%s] " format, (dev)->ifname, ## __VA_ARGS__)
@@ -122,6 +126,7 @@ struct kefa_data {
 	struct kefa_sched **scheds;		/* global schedulers */
 	struct kefa_cm_deamon **cm_daemons;	/* Connection manager daemons */
 	struct rhashtable peer_ni;
+	atomic_t peer_ni_count;
 	atomic_t nthreads;			/* # live threads */
 	bool shutdown;				/* signal shutdown to threads */
 };
@@ -180,7 +185,6 @@ struct kefa_fmr {
 	struct list_head list_node;
 	struct ib_reg_wr reg_wr;
 	struct ib_send_wr inv_wr;
-	struct kefa_qp *inv_qp;
 };
 
 /* EFA device information */
@@ -220,7 +224,6 @@ struct kefa_tx {
 	u32 lkey;				/* lkey of sge buffers */
 	enum lnet_msg_hstatus hstatus;		/* health status of tx */
 	int status;				/* overall status */
-	struct kefa_qp *qp;			/* posted to QP */
 	int nfrags;				/* # of mapped buffer fragments */
 	struct scatterlist *frags;		/* mapped buffer fragments */
 	struct kefa_rdma_desc rdma_desc;	/* rdma descriptor to read/write */
@@ -339,20 +342,23 @@ kefalnd_thread_stop(void)
 static inline void
 kefalnd_init_msg(struct kefa_msg *msg, int type, int body_nob)
 {
-	msg->type = type;
-	msg->nob = offsetof(struct kefa_msg, u) + body_nob;
+	msg->hdr.type = type;
+	msg->hdr.nob = offsetof(struct kefa_msg, msg_v1.u) + body_nob;
 }
 
 static inline void
 kefalnd_msg_fill_hdr(struct kefa_conn *conn, struct kefa_msg *msg, u8 proto_ver)
 {
-	msg->magic = EFALND_MSG_MAGIC;
-	msg->proto_ver = proto_ver;
-	msg->credits = 0;
-	msg->srcnid = lnet_nid_to_nid4(&conn->local_nid);
-	msg->dstnid = lnet_nid_to_nid4(&conn->remote_nid);
-	msg->dst_epoch = conn->remote_epoch;
-	msg->dst_conn_id = EFALND_INV_CONN;
+	struct kefa_msg_v1 *msg_v1 = &msg->msg_v1;
+
+	msg->hdr.magic = EFALND_MSG_MAGIC;
+	msg->hdr.proto_ver = proto_ver;
+
+	msg_v1->credits = 0;
+	msg_v1->srcnid = lnet_nid_to_nid4(&conn->local_nid);
+	msg_v1->dstnid = lnet_nid_to_nid4(&conn->remote_nid);
+	msg_v1->dst_epoch = conn->remote_epoch;
+	msg_v1->dst_conn_id = EFALND_INV_CONN;
 }
 
 static inline int kefalnd_dma_map_sg(struct kefa_dev *efa_dev,
@@ -385,63 +391,21 @@ static inline void kefalnd_dma_unmap_sg(struct kefa_dev *efa_dev,
 	ib_dma_unmap_sg(efa_dev->ib_dev, sg, nents, direction);
 }
 
-static inline u64
-kefalnd_rdma_frag_addr(struct kefa_rdma_desc *rd)
-{
-	return rd->frag.addr;
-}
-
-static inline u32
-kefalnd_rdma_frag_size(struct kefa_rdma_desc *rd)
-{
-	return rd->frag.nob;
-}
-
-static inline u32
-kefalnd_rdma_frag_key(struct kefa_rdma_desc *rd)
-{
-	return rd->key;
-}
-
-static inline int
-kefalnd_rdma_consume_frag(struct kefa_rdma_desc *rd, int index, u32 nob)
-{
-	if (nob < rd->frag.nob) {
-		rd->frag.addr += nob;
-		rd->frag.nob -= nob;
-	} else {
-		index++;
-	}
-
-	return index;
-}
-
-static inline void
-kefalnd_rdma_dump(struct kefa_rdma_desc *rd)
-{
-	CERROR("cpu[%u] key[0x%x]\n", smp_processor_id(), rd->key);
-	CERROR("cpu[%u] addr[%p] len[%u]\n", smp_processor_id(), (void *)rd->frag.addr,
-	       rd->frag.nob);
-}
-
-static inline u32
-kefalnd_addr_to_dev_num(u32 addr)
-{
-	return (addr & 0xffff);
-}
-
 int kefalnd_tunables_init(void);
 int kefalnd_tunables_setup(struct lnet_ni *ni);
 
+int kefalnd_msgtype2size(int type);
 int kefalnd_efa_status_to_errno(s16 efa_status);
 s16 kefalnd_errno_to_efa_status(int status);
 void kefalnd_tx_done(struct kefa_tx *tx);
-void kefalnd_abort_tx_locked(struct kefa_tx *tx, enum lnet_msg_hstatus hstatus, int status);
+void kefalnd_abort_tx(struct kefa_tx *tx, enum lnet_msg_hstatus hstatus, int status);
 /* Should be used only on TXs that we don't expect to get any completions for */
-void kefalnd_force_cancel_tx_locked(struct kefa_tx *tx, enum lnet_msg_hstatus hstatus, int status);
-void kefalnd_init_tx_msg(struct kefa_tx *tx, int type, int body_nob);
+void kefalnd_force_cancel_tx(struct kefa_tx *tx, enum lnet_msg_hstatus hstatus, int status);
+void kefalnd_init_tx_protocol_msg(struct kefa_tx *tx, int type, int body_nob);
 struct kefa_tx *kefalnd_get_idle_tx(struct kefa_ni *efa_ni);
 void kefalnd_conn_post_tx_locked(struct kefa_conn *conn);
+void kefalnd_get_srcnid_from_msg(struct kefa_msg *msg, struct lnet_nid *srcnid);
+void kefalnd_get_dstnid_from_msg(struct kefa_msg *msg, struct lnet_nid *dstnid);
 
 struct kefa_peer_ni *kefalnd_find_remote_peer_ni(struct kefa_dev *efa_dev,
 						 struct lnet_nid *efa_nid);
@@ -450,22 +414,21 @@ struct kefa_peer_ni *kefalnd_lookup_or_create_peer_ni(u32 nid_addr, union ib_gid
 void kefalnd_update_peer_ni(struct kefa_peer_ni *peer_ni, union ib_gid *gid,
 			    u16 cm_qpn, u32 cm_qkey);
 int kefalnd_get_nid_metadata(struct lnet_ni *ni, struct lnet_nid_md_entry *md_entry);
-int kefalnd_peerni_init(void);
-void kefalnd_peerni_exit(void);
 lnet_nid_t kefalnd_create_efa_nid(u32 ip_addr, u32 bus_num, u32 devfn);
 void kefalnd_put_peer_ni(struct kefa_peer_ni *peer_ni);
 
-struct kefa_conn *kefalnd_get_conn_from_rx(struct kefa_ni *efa_ni, struct kefa_rx *rx,
-					   enum kefa_conn_type conn_type);
+void kefalnd_debugfs_init(void);
+void kefalnd_debugfs_exit(void);
+
+struct kefa_conn *kefalnd_lookup_conn(struct kefa_ni *efa_ni, struct lnet_nid *nid,
+				      enum kefa_conn_type conn_type);
 struct kefa_conn *kefalnd_lookup_or_init_conn(struct kefa_ni *efa_ni, struct lnet_nid *nid,
 					      enum kefa_conn_type conn_type);
-int kefalnd_handle_conn_probe(struct kefa_ni *efa_ni, struct kefa_rx *rx);
-int kefalnd_handle_conn_probe_resp(struct kefa_ni *efa_ni, struct kefa_rx *rx);
-int kefalnd_handle_conn_req(struct kefa_ni *efa_ni, struct kefa_rx *rx);
-int kefalnd_handle_conn_req_ack(struct kefa_ni *efa_ni, struct kefa_rx *rx);
+void kefalnd_handle_conn_establishment(struct kefa_ni *efa_ni, struct kefa_msg *msg);
 void kefalnd_deactivate_conn(struct kefa_conn *conn);
 void kefalnd_destroy_conn(struct kefa_conn *conn, enum lnet_msg_hstatus hstatus, int status);
 int kefalnd_cm_daemon(void *arg);
 void kefalnd_add_ni_to_cm_daemon(struct kefa_ni *efa_ni);
 void kefalnd_del_ni_from_cm_daemon(struct kefa_ni *efa_ni);
+
 #endif
diff --git a/net/lnet/lnet/klnds/efalnd/efalnd_connection.c b/net/lnet/lnet/klnds/efalnd/efalnd_connection.c
--- a/net/lnet/lnet/klnds/efalnd/efalnd_connection.c
+++ b/net/lnet/lnet/klnds/efalnd/efalnd_connection.c
@@ -1,7 +1,7 @@
 // SPDX-License-Identifier: GPL-2.0
 
 /*
- * Copyright (c) 2024, Amazon and/or its affiliates. All rights reserved.
+ * Copyright (c) 2024-2025, Amazon and/or its affiliates. All rights reserved.
  * Use is subject to license terms.
  */
 
@@ -20,6 +20,42 @@
 #define MAX_IDLE_CONN		32
 #define RESP_CONN_EXTRA_TIME	300
 
+static inline struct kefa_conn_probe_msg *
+kefalnd_get_probe_from_msg(struct kefa_msg *msg)
+{
+	if (msg->hdr.proto_ver != EFALND_PROTO_VER_1)
+		return &msg->msg_v2.u.conn_probe;
+
+	return &msg->msg_v1.u.conn_probe;
+}
+
+static inline struct kefa_conn_probe_resp_msg *
+kefalnd_get_probe_resp_from_msg(struct kefa_msg *msg)
+{
+	if (msg->hdr.proto_ver != EFALND_PROTO_VER_1)
+		return &msg->msg_v2.u.conn_probe_resp;
+
+	return &msg->msg_v1.u.conn_probe_resp;
+}
+
+static inline struct kefa_conn_req_msg *
+kefalnd_get_conn_req_from_msg(struct kefa_msg *msg)
+{
+	if (msg->hdr.proto_ver != EFALND_PROTO_VER_1)
+		return &msg->msg_v2.u.conn_request;
+
+	return &msg->msg_v1.u.conn_request;
+}
+
+static inline struct kefa_conn_req_ack *
+kefalnd_get_conn_req_ack_from_msg(struct kefa_msg *msg)
+{
+	if (msg->hdr.proto_ver != EFALND_PROTO_VER_1)
+		return &msg->msg_v2.u.conn_request_ack;
+
+	return &msg->msg_v1.u.conn_request_ack;
+}
+
 static inline void
 kefalnd_set_conn_state_locked(struct kefa_conn *conn, enum kefa_conn_state new_state)
 __must_hold(&conn->lock)
@@ -45,13 +81,13 @@ kefalnd_post_conn_message(struct kefa_conn *conn, struct kefa_tx *tx)
 {
 	struct kefa_dev *efa_dev = conn->efa_ni->efa_dev;
 	struct kefa_peer_ni *peer_ni = conn->peer_ni;
+	struct kefa_qp *cm_qp = efa_dev->cm_qp;
 	struct ib_srd_wr *srd_wr;
 	struct ib_send_wr *wr;
 	unsigned long flags;
 	int rc;
 
 	tx->conn = conn;
-	tx->qp = efa_dev->cm_qp;
 
 	srd_wr = &tx->wrq.wr;
 	wr = &srd_wr->wr;
@@ -71,28 +107,22 @@ kefalnd_post_conn_message(struct kefa_conn *conn, struct kefa_tx *tx)
 	srd_wr->remote_qkey = peer_ni->cm_qp.qkey;
 	read_unlock_irqrestore(&peer_ni->peer_ni_lock, flags);
 
-	rc = ib_post_send(tx->qp->ib_qp, wr, NULL);
+	rc = ib_post_send(cm_qp->ib_qp, wr, NULL);
 	if (rc) {
 		EFA_DEV_ERR(efa_dev, "QP[%u] failed to post conn msg to peer NI[%s]. err[%d]\n",
-			    tx->qp->ib_qp->qp_num, libcfs_nidstr(&conn->remote_nid), rc);
+			    cm_qp->ib_qp->qp_num, libcfs_nidstr(&conn->remote_nid), rc);
 
 		spin_lock_irqsave(&conn->lock, flags);
 		list_del_init(&tx->list_node);
 		spin_unlock_irqrestore(&conn->lock, flags);
 
 		tx->conn = NULL;
-		tx->qp = NULL;
 		atomic_set(&tx->ref_cnt, 0);
 		atomic64_set(&tx->send_time, 0);
 
 		return rc;
 	}
 
-	/* TT: https://issues.amazon.com/issues/EFA-3459 */
-	rc = ib_req_notify_cq(tx->qp->cq->ib_cq, IB_CQ_NEXT_COMP);
-	if (rc < 0)
-		EFA_DEV_ERR(tx->qp->cq->efa_dev, "request notify CQ failed. err[%d]\n", rc);
-
 	return 0;
 }
 
@@ -117,10 +147,8 @@ kefalnd_send_conn_req_ack(struct kefa_conn *conn, int status)
 {
 	struct kefa_dev *efa_dev = conn->efa_ni->efa_dev;
 	struct kefa_conn_req_ack *ack_msg;
-	struct kefa_msg *msg;
 	struct kefa_tx *tx;
-	int nob;
-	int rc;
+	int nob, rc;
 
 	CDEBUG(D_NET, "Send connection ack from[%s] to[%s]\n",
 	       libcfs_nidstr(&conn->local_nid), libcfs_nidstr(&conn->remote_nid));
@@ -133,8 +161,7 @@ kefalnd_send_conn_req_ack(struct kefa_conn *conn, int status)
 		return -ENOMEM;
 	}
 
-	msg = tx->msg;
-	ack_msg = &msg->u.conn_request_ack;
+	ack_msg = &tx->msg->msg_v1.u.conn_request_ack;
 	ack_msg->lnd_ver = kefalnd_get_lnd_version();
 	ack_msg->src_epoch = conn->efa_ni->ni_epoch;
 	ack_msg->caps = 0;
@@ -150,7 +177,7 @@ kefalnd_send_conn_req_ack(struct kefa_conn *conn, int status)
 		ack_msg->nqps = kefalnd_select_conn_qps(efa_dev, &ack_msg->data_qps[0]);
 	}
 
-	kefalnd_init_tx_msg(tx, EFALND_MSG_CONN_REQ_ACK, nob);
+	kefalnd_init_tx_protocol_msg(tx, EFALND_MSG_CONN_REQ_ACK, nob);
 
 	rc = kefalnd_post_conn_message(conn, tx);
 	if (rc)
@@ -164,10 +191,8 @@ kefalnd_send_conn_req(struct kefa_conn *conn)
 {
 	struct kefa_dev *efa_dev = conn->efa_ni->efa_dev;
 	struct kefa_conn_req_msg *req;
-	struct kefa_msg *msg;
 	struct kefa_tx *tx;
-	int nob;
-	int rc;
+	int nob, rc;
 
 	LASSERT(conn->state == KEFA_CONN_ESTABLISH);
 
@@ -183,10 +208,9 @@ kefalnd_send_conn_req(struct kefa_conn *conn)
 	}
 
 	nob = offsetof(struct kefa_conn_req_msg, data_qps[efa_dev->nqps]);
-	kefalnd_init_tx_msg(tx, EFALND_MSG_CONN_REQ, nob);
+	kefalnd_init_tx_protocol_msg(tx, EFALND_MSG_CONN_REQ, nob);
 
-	msg = tx->msg;
-	req = &msg->u.conn_request;
+	req = &tx->msg->msg_v1.u.conn_request;
 	req->lnd_ver = kefalnd_get_lnd_version();
 	memcpy(req->src_gid, efa_dev->gid.raw, sizeof(req->src_gid));
 	req->src_epoch = conn->efa_ni->ni_epoch;
@@ -211,7 +235,6 @@ kefalnd_send_conn_probe_resp(struct kefa_conn *conn, int status)
 {
 	struct kefa_dev *efa_dev = conn->efa_ni->efa_dev;
 	struct kefa_conn_probe_resp_msg *probe_resp;
-	struct kefa_msg *msg;
 	struct kefa_tx *tx;
 	int rc;
 
@@ -226,14 +249,15 @@ kefalnd_send_conn_probe_resp(struct kefa_conn *conn, int status)
 		return -ENOMEM;
 	}
 
-	kefalnd_init_tx_msg(tx, EFALND_MSG_CONN_PROBE_RESP,
-			    sizeof(struct kefa_conn_probe_resp_msg));
+	kefalnd_init_tx_protocol_msg(tx, EFALND_MSG_CONN_PROBE_RESP,
+				     sizeof(struct kefa_conn_probe_resp_msg));
 
-	msg = tx->msg;
-	probe_resp = &msg->u.conn_probe_resp;
+	probe_resp = &tx->msg->msg_v1.u.conn_probe_resp;
 	probe_resp->lnd_ver = kefalnd_get_lnd_version();
 	probe_resp->src_epoch = conn->efa_ni->ni_epoch;
 	probe_resp->caps = 0;
+	probe_resp->min_proto_ver = EFALND_MIN_PROTO_VER;
+	probe_resp->max_proto_ver = EFALND_MAX_PROTO_VER;
 	probe_resp->status = kefalnd_errno_to_efa_status(status);
 
 	rc = kefalnd_post_conn_message(conn, tx);
@@ -248,7 +272,6 @@ kefalnd_send_conn_probe(struct kefa_conn *conn)
 {
 	struct kefa_dev *efa_dev = conn->efa_ni->efa_dev;
 	struct kefa_conn_probe_msg *probe;
-	struct kefa_msg *msg;
 	struct kefa_tx *tx;
 	int rc;
 
@@ -265,10 +288,9 @@ kefalnd_send_conn_probe(struct kefa_conn *conn)
 		return -ENOMEM;
 	}
 
-	kefalnd_init_tx_msg(tx, EFALND_MSG_CONN_PROBE, sizeof(struct kefa_conn_probe_msg));
+	kefalnd_init_tx_protocol_msg(tx, EFALND_MSG_CONN_PROBE, sizeof(struct kefa_conn_probe_msg));
 
-	msg = tx->msg;
-	probe = &msg->u.conn_probe;
+	probe = &tx->msg->msg_v1.u.conn_probe;
 	probe->lnd_ver = kefalnd_get_lnd_version();
 	probe->src_epoch = conn->efa_ni->ni_epoch;
 	memcpy(probe->src_gid, efa_dev->gid.raw, sizeof(probe->src_gid));
@@ -287,21 +309,24 @@ void
 kefalnd_destroy_conn(struct kefa_conn *conn, enum lnet_msg_hstatus hstatus, int status)
 {
 	struct kefa_tx *tx, *temp_tx;
+	struct list_head cancel_tx;
 	unsigned long flags;
 
-	spin_lock_irqsave(&conn->lock, flags);
+	INIT_LIST_HEAD(&cancel_tx);
 
+	spin_lock_irqsave(&conn->lock, flags);
 	if (!list_empty(&conn->active_tx))
 		EFA_DEV_WARN(conn->efa_ni->efa_dev,
 			     "destroying conn to peer NI[%s] with active TXs\n",
 			     libcfs_nidstr(&conn->remote_nid));
 
-	list_for_each_entry_safe(tx, temp_tx, &conn->pend_tx, list_node) {
-		kefalnd_force_cancel_tx_locked(tx, hstatus, status);
-	}
-
+	list_splice_init(&conn->pend_tx, &cancel_tx);
 	spin_unlock_irqrestore(&conn->lock, flags);
 
+	list_for_each_entry_safe(tx, temp_tx, &cancel_tx, list_node) {
+		kefalnd_force_cancel_tx(tx, hstatus, status);
+	}
+
 	if (!IS_ERR_OR_NULL(conn->ah))
 		rdma_destroy_ah(conn->ah, RDMA_DESTROY_AH_SLEEPABLE);
 
@@ -315,7 +340,7 @@ kefalnd_destroy_conn(struct kefa_conn *conn, enum lnet_msg_hstatus hstatus, int
 }
 
 static struct kefa_conn *
-kefalnd_create_conn(struct kefa_ni *efa_ni, struct lnet_nid *nid_peer,
+kefalnd_create_conn(struct kefa_ni *efa_ni, struct lnet_nid *peer_nid,
 		    enum kefa_conn_type conn_type)
 {
 	struct kefa_dev *efa_dev = efa_ni->efa_dev;
@@ -328,8 +353,8 @@ kefalnd_create_conn(struct kefa_ni *efa_ni, struct lnet_nid *nid_peer,
 	}
 
 	conn->efa_ni = efa_ni;
-	conn->proto_ver = EFALND_CURR_PROTO_VER;
-	conn->remote_nid = *nid_peer;
+	conn->proto_ver = EFALND_MAX_PROTO_VER;
+	conn->remote_nid = *peer_nid;
 	conn->local_nid = efa_ni->lnet_ni->ni_nid;
 	conn->remote_epoch = 0;
 	conn->last_use_time = ktime_get_seconds();
@@ -431,8 +456,8 @@ kefalnd_establish_conn(struct kefa_conn *conn)
 	kefalnd_set_conn_state(conn, KEFA_CONN_PROBE_TCP);
 	peer_ni = kefalnd_find_remote_peer_ni(efa_dev, &conn->remote_nid);
 	if (IS_ERR_OR_NULL(peer_ni)) {
-		EFA_DEV_ERR(efa_dev, "failed to locate GID for NID %s. err[%ld]\n",
-			    libcfs_nidstr(&conn->remote_nid), PTR_ERR(peer_ni));
+		EFA_DEV_DEBUG(efa_dev, "failed to locate GID for NID %s. err[%ld]\n",
+			      libcfs_nidstr(&conn->remote_nid), PTR_ERR(peer_ni));
 		return PTR_ERR(peer_ni);
 	}
 
@@ -578,38 +603,33 @@ kefalnd_lookup_or_init_conn(struct kefa_ni *efa_ni, struct lnet_nid *nid,
 }
 
 struct kefa_conn *
-kefalnd_get_conn_from_rx(struct kefa_ni *efa_ni, struct kefa_rx *rx, enum kefa_conn_type conn_type)
+kefalnd_lookup_conn(struct kefa_ni *efa_ni, struct lnet_nid *nid, enum kefa_conn_type conn_type)
 {
-	struct kefa_msg *msg = rx->msg;
 	struct kefa_conn *conn;
-	struct lnet_nid nid;
 	unsigned long flags;
 
-	lnet_nid4_to_nid(msg->srcnid, &nid);
-
 	read_lock_irqsave(&efa_ni->conn_lock, flags);
-	conn = kefalnd_lookup_conn_locked(efa_ni, &nid, conn_type);
+	conn = kefalnd_lookup_conn_locked(efa_ni, nid, conn_type);
 	read_unlock_irqrestore(&efa_ni->conn_lock, flags);
 	return conn;
 }
 
-int
-kefalnd_handle_conn_req_ack(struct kefa_ni *efa_ni, struct kefa_rx *rx)
+static int
+kefalnd_handle_conn_req_ack(struct kefa_ni *efa_ni, struct lnet_nid *srcnid,
+			    struct lnet_nid *dstnid, struct kefa_conn_req_ack *ack_msg)
 {
-	struct kefa_msg *msg = rx->msg;
-	struct kefa_conn_req_ack *ack_msg;
 	enum lnet_msg_hstatus hstatus;
 	struct kefa_conn *conn;
 	unsigned long flags;
 	int rc;
 
 	CDEBUG(D_NET, "Received connection ack from[%s] to[%s]\n",
-	       libcfs_nid2str(msg->srcnid), libcfs_nid2str(msg->dstnid));
+	       libcfs_nidstr(srcnid), libcfs_nidstr(dstnid));
 
-	conn = kefalnd_get_conn_from_rx(efa_ni, rx, KEFA_CONN_TYPE_INITIATOR);
+	conn = kefalnd_lookup_conn(efa_ni, srcnid, KEFA_CONN_TYPE_INITIATOR);
 	if (!conn) {
 		EFA_DEV_ERR(efa_ni->efa_dev, "Unexpected connection from[%s] to[%s]\n",
-			    libcfs_nid2str(msg->srcnid), libcfs_nid2str(msg->dstnid));
+			    libcfs_nidstr(srcnid), libcfs_nidstr(dstnid));
 
 		return -ENOTCONN;
 	}
@@ -618,20 +638,19 @@ kefalnd_handle_conn_req_ack(struct kefa_ni *efa_ni, struct kefa_rx *rx)
 	if (conn->state != KEFA_CONN_ESTABLISH) {
 		spin_unlock_irqrestore(&conn->lock, flags);
 		EFA_DEV_ERR(efa_ni->efa_dev, "Unexpected connection state[%u] from[%s] to[%s]\n",
-			    conn->state, libcfs_nid2str(msg->srcnid), libcfs_nid2str(msg->dstnid));
+			    conn->state, libcfs_nidstr(srcnid), libcfs_nidstr(dstnid));
 
 		return -EINVAL;
 	}
 
 	spin_unlock_irqrestore(&conn->lock, flags);
 
-	ack_msg = &msg->u.conn_request_ack;
 	if (ack_msg->status) {
 		rc = kefalnd_efa_status_to_errno(ack_msg->status);
 		hstatus = LNET_MSG_STATUS_REMOTE_DROPPED;
 		EFA_DEV_ERR(efa_ni->efa_dev,
 			    "conn request failed on remote err[%d] from[%s] to[%s]\n",
-			    rc, libcfs_nid2str(msg->srcnid), libcfs_nid2str(msg->dstnid));
+			    rc, libcfs_nidstr(srcnid), libcfs_nidstr(dstnid));
 
 		goto cleanup_conn;
 	}
@@ -658,22 +677,22 @@ kefalnd_handle_conn_req_ack(struct kefa_ni *efa_ni, struct kefa_rx *rx)
 	return rc;
 }
 
-int
-kefalnd_handle_conn_req(struct kefa_ni *efa_ni, struct kefa_rx *rx)
+static int
+kefalnd_handle_conn_req(struct kefa_ni *efa_ni, struct lnet_nid *srcnid,
+			struct lnet_nid *dstnid, struct kefa_conn_req_msg *request_msg,
+			u8 proto_ver)
 {
-	struct kefa_conn_req_msg *request_msg;
-	struct kefa_msg *msg = rx->msg;
 	struct kefa_conn *conn;
 	unsigned long flags;
 	int rc;
 
 	CDEBUG(D_NET, "Received connection request from[%s] to[%s]\n",
-	       libcfs_nid2str(msg->srcnid), libcfs_nid2str(msg->dstnid));
+	       libcfs_nidstr(srcnid), libcfs_nidstr(dstnid));
 
-	conn = kefalnd_get_conn_from_rx(efa_ni, rx, KEFA_CONN_TYPE_RESPONDER);
+	conn = kefalnd_lookup_conn(efa_ni, srcnid, KEFA_CONN_TYPE_RESPONDER);
 	if (!conn) {
 		EFA_DEV_ERR(efa_ni->efa_dev, "Unexpected connection from[%s] to[%s]\n",
-			    libcfs_nid2str(msg->srcnid), libcfs_nid2str(msg->dstnid));
+			    libcfs_nidstr(srcnid), libcfs_nidstr(dstnid));
 
 		return -ENOTCONN;
 	}
@@ -681,14 +700,13 @@ kefalnd_handle_conn_req(struct kefa_ni *efa_ni, struct kefa_rx *rx)
 	/* We currently asume connection probe comes before request so the connection must exits
 	 * at this point and it can be active or during establishment.
 	 */
-	request_msg = &msg->u.conn_request;
-
 	spin_lock_irqsave(&conn->lock, flags);
 	switch (conn->state) {
 	case KEFA_CONN_PROBE_EFA_PASSIVE:
 		/* Regular path */
 		/* TODO: validate requests make sense */
 		conn->requests = request_msg->requests;
+		conn->proto_ver = proto_ver;
 
 		rc = kefalnd_init_conn_data_qps(conn, (struct kefa_qp_proto *)request_msg->data_qps,
 						request_msg->nqps);
@@ -710,7 +728,7 @@ kefalnd_handle_conn_req(struct kefa_ni *efa_ni, struct kefa_rx *rx)
 	default:
 		spin_unlock_irqrestore(&conn->lock, flags);
 		EFA_DEV_ERR(efa_ni->efa_dev, "Unexpected connection state[%u] from[%s] to[%s]\n",
-			    conn->state, libcfs_nid2str(msg->srcnid), libcfs_nid2str(msg->dstnid));
+			    conn->state, libcfs_nidstr(srcnid), libcfs_nidstr(dstnid));
 
 		return -EINVAL;
 	}
@@ -719,23 +737,25 @@ kefalnd_handle_conn_req(struct kefa_ni *efa_ni, struct kefa_rx *rx)
 	return rc;
 }
 
-int
-kefalnd_handle_conn_probe_resp(struct kefa_ni *efa_ni, struct kefa_rx *rx)
+static int
+kefalnd_handle_conn_probe_resp(struct kefa_ni *efa_ni, struct lnet_nid *srcnid,
+			       struct lnet_nid *dstnid,
+			       struct kefa_conn_probe_resp_msg *probe_resp_msg,
+			       u16 msg_nob)
 {
-	struct kefa_conn_probe_resp_msg *probe_resp_msg;
-	struct kefa_msg *msg = rx->msg;
 	enum lnet_msg_hstatus hstatus;
+	u8 min_proto, max_proto;
 	struct kefa_conn *conn;
 	unsigned long flags;
 	int rc;
 
 	CDEBUG(D_NET, "Received connection probe response from[%s] to[%s]\n",
-	       libcfs_nid2str(msg->srcnid), libcfs_nid2str(msg->dstnid));
+	       libcfs_nidstr(srcnid), libcfs_nidstr(dstnid));
 
-	conn = kefalnd_get_conn_from_rx(efa_ni, rx, KEFA_CONN_TYPE_INITIATOR);
+	conn = kefalnd_lookup_conn(efa_ni, srcnid, KEFA_CONN_TYPE_INITIATOR);
 	if (!conn) {
 		EFA_DEV_ERR(efa_ni->efa_dev, "Unexpected connection from[%s] to[%s]\n",
-			    libcfs_nid2str(msg->srcnid), libcfs_nid2str(msg->dstnid));
+			    libcfs_nidstr(srcnid), libcfs_nidstr(dstnid));
 
 		return -ENOTCONN;
 	}
@@ -744,19 +764,34 @@ kefalnd_handle_conn_probe_resp(struct kefa_ni *efa_ni, struct kefa_rx *rx)
 	if (conn->state != KEFA_CONN_PROBE_EFA) {
 		spin_unlock_irqrestore(&conn->lock, flags);
 		EFA_DEV_ERR(efa_ni->efa_dev, "Unexpected connection state[%u] from[%s] to[%s]\n",
-			    conn->state, libcfs_nid2str(msg->srcnid), libcfs_nid2str(msg->dstnid));
+			    conn->state, libcfs_nidstr(srcnid), libcfs_nidstr(dstnid));
 
 		return -EINVAL;
 	}
 
 	spin_unlock_irqrestore(&conn->lock, flags);
 
-	probe_resp_msg = &msg->u.conn_probe_resp;
+	min_proto = EFALND_PROTO_VER_1;
+	max_proto = EFALND_PROTO_VER_1;
+	if (EFALND_FIELD_AVAIL(typeof(*probe_resp_msg), max_proto_ver, msg_nob)) {
+		min_proto = probe_resp_msg->min_proto_ver;
+		max_proto = probe_resp_msg->max_proto_ver;
+	}
+
 	rc = kefalnd_efa_status_to_errno(probe_resp_msg->status);
 	if (rc) {
-		/* Downgrade protocol version to remote's supported version and probe again. */
 		if (rc == -EPROTONOSUPPORT) {
-			conn->proto_ver = msg->proto_ver;
+			/* Check for supported proto versions overlap */
+			if (min_t(u8, EFALND_MAX_PROTO_VER, max_proto) <
+			    max_t(u8, EFALND_MIN_PROTO_VER, min_proto)) {
+				hstatus = LNET_MSG_STATUS_LOCAL_ERROR;
+				goto cleanup_conn;
+			}
+
+			/* Downgrade protocol version to remote's supported version
+			 * and probe again.
+			 */
+			conn->proto_ver = min_t(u8, EFALND_MAX_PROTO_VER, max_proto);
 			rc = kefalnd_send_conn_probe(conn);
 			if (!rc)
 				return 0;
@@ -773,6 +808,7 @@ kefalnd_handle_conn_probe_resp(struct kefa_ni *efa_ni, struct kefa_rx *rx)
 	conn->remote_caps = probe_resp_msg->caps;
 	conn->requests = 0;
 	conn->remote_epoch = probe_resp_msg->src_epoch;
+	conn->proto_ver = min_t(u8, EFALND_MAX_PROTO_VER, max_proto);
 
 	kefalnd_set_conn_state(conn, KEFA_CONN_ESTABLISH);
 	rc = kefalnd_send_conn_req(conn);
@@ -853,28 +889,25 @@ kefalnd_refresh_connection(struct kefa_ni *efa_ni, struct kefa_conn *old_conn,
 	return new_conn;
 }
 
-int
-kefalnd_handle_conn_probe(struct kefa_ni *efa_ni, struct kefa_rx *rx)
+static int
+kefalnd_handle_conn_probe(struct kefa_ni *efa_ni, struct lnet_nid *srcnid,
+			  struct lnet_nid *dstnid, struct kefa_conn_probe_msg *probe_msg,
+			  u8 proto_ver)
 {
-	struct kefa_conn_probe_msg *probe_msg;
 	struct kefa_conn *conn, *init_conn;
-	struct kefa_msg *msg = rx->msg;
 	struct kefa_peer_ni *peer_ni;
 	bool init_conn_active;
-	struct lnet_nid nid;
 	unsigned long flags;
 	union ib_gid gid;
 	int rc, resp_rc;
 
 	CDEBUG(D_NET, "Received connection probe from[%s] to[%s]\n",
-	       libcfs_nid2str(msg->srcnid), libcfs_nid2str(msg->dstnid));
+	       libcfs_nidstr(srcnid), libcfs_nidstr(dstnid));
 
-	lnet_nid4_to_nid(msg->srcnid, &nid);
-	conn = kefalnd_lookup_or_init_conn(efa_ni, &nid, KEFA_CONN_TYPE_RESPONDER);
+	conn = kefalnd_lookup_or_init_conn(efa_ni, srcnid, KEFA_CONN_TYPE_RESPONDER);
 	if (!conn)
 		return PTR_ERR(conn);
 
-	probe_msg = &msg->u.conn_probe;
 	memcpy(gid.raw, probe_msg->src_gid, sizeof(probe_msg->src_gid));
 
 	spin_lock_irqsave(&conn->lock, flags);
@@ -882,7 +915,7 @@ kefalnd_handle_conn_probe(struct kefa_ni *efa_ni, struct kefa_rx *rx)
 	case KEFA_CONN_INACTIVE:
 		kefalnd_set_conn_state_locked(conn, KEFA_CONN_PROBE_EFA_PASSIVE);
 		spin_unlock_irqrestore(&conn->lock, flags);
-		peer_ni = kefalnd_lookup_or_create_peer_ni(msg->srcnid, &gid,
+		peer_ni = kefalnd_lookup_or_create_peer_ni(lnet_nid_to_nid4(srcnid), &gid,
 							   probe_msg->cm_qp.qp_num,
 							   probe_msg->cm_qp.qkey);
 		if (IS_ERR_OR_NULL(peer_ni)) {
@@ -911,8 +944,8 @@ kefalnd_handle_conn_probe(struct kefa_ni *efa_ni, struct kefa_rx *rx)
 		}
 
 		spin_unlock_irqrestore(&conn->lock, flags);
-		conn = kefalnd_refresh_connection(efa_ni, conn, &nid, &gid, probe_msg->cm_qp.qp_num,
-						  probe_msg->cm_qp.qkey);
+		conn = kefalnd_refresh_connection(efa_ni, conn, srcnid, &gid,
+						  probe_msg->cm_qp.qp_num, probe_msg->cm_qp.qkey);
 		if (IS_ERR(conn)) {
 			return PTR_ERR(conn);
 		}
@@ -928,13 +961,13 @@ kefalnd_handle_conn_probe(struct kefa_ni *efa_ni, struct kefa_rx *rx)
 		 */
 		spin_unlock_irqrestore(&conn->lock, flags);
 		EFA_DEV_ERR(efa_ni->efa_dev, "Unexpected connection state[%u] from[%s] to[%s]\n",
-			    conn->state, libcfs_nid2str(msg->srcnid), libcfs_nid2str(msg->dstnid));
+			    conn->state, libcfs_nidstr(srcnid), libcfs_nidstr(dstnid));
 
 		return -EINVAL;
 	}
 
 	/* We check if TX connection is valid as well and if not remove it */
-	init_conn = kefalnd_get_conn_from_rx(efa_ni, rx, KEFA_CONN_TYPE_INITIATOR);
+	init_conn = kefalnd_lookup_conn(efa_ni, srcnid, KEFA_CONN_TYPE_INITIATOR);
 	if (init_conn) {
 		spin_lock_irqsave(&init_conn->lock, flags);
 		init_conn_active = init_conn->state == KEFA_CONN_ACTIVE;
@@ -952,9 +985,9 @@ kefalnd_handle_conn_probe(struct kefa_ni *efa_ni, struct kefa_rx *rx)
 		goto cleanup_conn;
 
 proto_check:
-	resp_rc = msg->proto_ver > EFALND_CURR_PROTO_VER ? -EPROTONOSUPPORT : 0;
+	resp_rc = proto_ver > EFALND_MAX_PROTO_VER ? -EPROTONOSUPPORT : 0;
 	if (!resp_rc) {
-		conn->proto_ver = msg->proto_ver;
+		conn->proto_ver = proto_ver;
 	}
 
 	rc = kefalnd_send_conn_probe_resp(conn, resp_rc);
@@ -969,47 +1002,61 @@ kefalnd_handle_conn_probe(struct kefa_ni *efa_ni, struct kefa_rx *rx)
 	return rc;
 }
 
-/* TT: https://issues.amazon.com/issues/EFA-3459 */
-static void
-kefalnd_notify_all_cqs(struct kefa_ni *efa_ni)
+static inline u16
+kefalnd_get_payload_size_from_msg(struct kefa_msg *msg)
 {
-	struct kefa_dev *efa_dev = efa_ni->efa_dev;
-	struct kefa_sched *sched;
-	unsigned long flags;
-	int i, rc;
+	if (msg->hdr.proto_ver != EFALND_PROTO_VER_1)
+		return msg->hdr.nob - offsetof(struct kefa_msg, msg_v2.u);
 
-	if (hash_empty(efa_ni->conns))
-		return;
+	return msg->hdr.nob - offsetof(struct kefa_msg, msg_v1.u);
+}
 
-	sched = kefalnd.scheds[efa_ni->lnet_ni->ni_dev_cpt];
+void
+kefalnd_handle_conn_establishment(struct kefa_ni *efa_ni, struct kefa_msg *msg)
+{
+	struct lnet_nid srcnid, dstnid;
+	u8 proto_ver;
 
-	spin_lock_irqsave(&sched->lock, flags);
-	rc = ib_req_notify_cq(efa_dev->cm_cq->ib_cq, IB_CQ_NEXT_COMP);
-	if (rc < 0)
-		EFA_DEV_ERR(efa_dev, "request notify CM CQ failed. err[%d]\n", rc);
+	kefalnd_get_srcnid_from_msg(msg, &srcnid);
+	kefalnd_get_dstnid_from_msg(msg, &dstnid);
+	proto_ver = msg->hdr.proto_ver;
 
-	if (list_empty(&efa_dev->cm_cq->sched_node))
-		list_add_tail(&efa_dev->cm_cq->sched_node, &sched->pend_cqs);
+	switch (msg->hdr.type) {
+	default:
+		EFA_DEV_ERR(efa_ni->efa_dev, "bad EFALND establishment message type[%x]\n",
+			    msg->hdr.type);
 
-	for (i = 0; i < efa_dev->ncqs; i++) {
-		rc = ib_req_notify_cq(efa_dev->cqs[i].ib_cq, IB_CQ_NEXT_COMP);
-		if (rc < 0)
-			EFA_DEV_ERR(efa_dev, "request notify CQ failed. err[%d]\n", rc);
+		break;
 
-		if (list_empty(&efa_dev->cqs[i].sched_node))
-			list_add_tail(&efa_dev->cqs[i].sched_node, &sched->pend_cqs);
-	}
+	case EFALND_MSG_CONN_PROBE:
+		kefalnd_handle_conn_probe(efa_ni, &srcnid, &dstnid, kefalnd_get_probe_from_msg(msg),
+					  proto_ver);
+		break;
+
+	case EFALND_MSG_CONN_PROBE_RESP:
+		kefalnd_handle_conn_probe_resp(efa_ni, &srcnid, &dstnid,
+					       kefalnd_get_probe_resp_from_msg(msg),
+					       kefalnd_get_payload_size_from_msg(msg));
+		break;
 
-	spin_unlock_irqrestore(&sched->lock, flags);
+	case EFALND_MSG_CONN_REQ:
+		kefalnd_handle_conn_req(efa_ni, &srcnid, &dstnid,
+					kefalnd_get_conn_req_from_msg(msg), proto_ver);
+		break;
 
-	wake_up_all(&sched->waitq);
+	case EFALND_MSG_CONN_REQ_ACK:
+		kefalnd_handle_conn_req_ack(efa_ni, &srcnid, &dstnid,
+					    kefalnd_get_conn_req_ack_from_msg(msg));
+		break;
+	}
 }
 
 static void
-kefalnd_cleanup_conn_txs(struct kefa_conn *conn)
+kefalnd_cleanup_conn_txs(struct kefa_conn *conn, struct list_head *abort_tx,
+			 struct list_head *cancel_tx)
 {
-	time64_t now = ktime_get_seconds();
 	int timeout = lnet_get_lnd_timeout();
+	time64_t now = ktime_get_seconds();
 	struct kefa_tx *tx, *temp_tx;
 	unsigned long flags;
 
@@ -1022,13 +1069,10 @@ kefalnd_cleanup_conn_txs(struct kefa_conn *conn)
 		if (tx_send_time == 0 || now < tx_send_time + timeout)
 			break;
 
-		/* Its a zombie TX */
-		kefalnd_abort_tx_locked(tx, LNET_MSG_STATUS_NETWORK_TIMEOUT, -ETIMEDOUT);
+		list_move_tail(&tx->list_node, abort_tx);
 	}
 
-	list_for_each_entry_safe(tx, temp_tx, &conn->pend_tx, list_node) {
-		kefalnd_force_cancel_tx_locked(tx, LNET_MSG_STATUS_LOCAL_ABORTED, -ETIMEDOUT);
-	}
+	list_splice_init(&conn->pend_tx, cancel_tx);
 
 	spin_unlock_irqrestore(&conn->lock, flags);
 }
@@ -1039,6 +1083,8 @@ kefalnd_cleanup_ni_conns(struct kefa_ni *efa_ni)
 	struct kefa_conn *removed_conn[MAX_IDLE_CONN] = { 0 };
 	struct kefa_conn *idle_conn[MAX_IDLE_CONN] = { 0 };
 	int init_timeout, resp_timeout, timeout, bkt, i = 0;
+	struct list_head abort_tx, cancel_tx;
+	struct kefa_tx *tx, *temp_tx;
 	struct kefa_conn *conn;
 	unsigned long flags;
 	int num_removed = 0;
@@ -1046,6 +1092,8 @@ kefalnd_cleanup_ni_conns(struct kefa_ni *efa_ni)
 	time64_t now;
 
 	now = ktime_get_seconds();
+	INIT_LIST_HEAD(&abort_tx);
+	INIT_LIST_HEAD(&cancel_tx);
 	init_timeout = efa_ni->lnet_ni->ni_net->net_tunables.lct_peer_timeout;
 	resp_timeout = init_timeout + RESP_CONN_EXTRA_TIME;
 
@@ -1078,7 +1126,7 @@ kefalnd_cleanup_ni_conns(struct kefa_ni *efa_ni)
 			kefalnd_deactivate_conn_locked(idle_conn[i]);
 
 		if (idle_conn[i]->state == KEFA_CONN_DEACTIVATING)
-			kefalnd_cleanup_conn_txs(idle_conn[i]);
+			kefalnd_cleanup_conn_txs(idle_conn[i], &abort_tx, &cancel_tx);
 
 		if (now > idle_conn[i]->last_use_time + timeout &&
 		    list_empty(&idle_conn[i]->active_tx)) {
@@ -1090,6 +1138,14 @@ kefalnd_cleanup_ni_conns(struct kefa_ni *efa_ni)
 
 	write_unlock_irqrestore(&efa_ni->conn_lock, flags);
 
+	list_for_each_entry_safe(tx, temp_tx, &abort_tx, list_node) {
+		kefalnd_abort_tx(tx, LNET_MSG_STATUS_NETWORK_TIMEOUT, -ETIMEDOUT);
+	}
+
+	list_for_each_entry_safe(tx, temp_tx, &cancel_tx, list_node) {
+		kefalnd_force_cancel_tx(tx, LNET_MSG_STATUS_LOCAL_ABORTED, -ETIMEDOUT);
+	}
+
 	for (i = 0; i < num_removed; i++)
 		kefalnd_destroy_conn(removed_conn[i], LNET_MSG_STATUS_LOCAL_ABORTED, -ESHUTDOWN);
 }
@@ -1115,7 +1171,6 @@ kefalnd_cm_daemon(void *arg)
 	while (!kefalnd.shutdown) {
 		mutex_lock(&cm_daemon->ni_list_lock);
 		list_for_each_entry(efa_ni, &cm_daemon->efa_ni_list, cm_node) {
-			kefalnd_notify_all_cqs(efa_ni);
 			if (cm_daemon->iter % 5 == 0)
 				kefalnd_cleanup_ni_conns(efa_ni);
 		}
diff --git a/net/lnet/lnet/klnds/efalnd/efalnd_debugfs.c b/net/lnet/lnet/klnds/efalnd/efalnd_debugfs.c
new file mode 100644
--- /dev/null
+++ b/net/lnet/lnet/klnds/efalnd/efalnd_debugfs.c
@@ -0,0 +1,90 @@
+// SPDX-License-Identifier: GPL-2.0
+
+/*
+ * Copyright (c) 2024, Amazon and/or its affiliates. All rights reserved.
+ * Use is subject to license terms.
+ */
+
+/*
+ * This file is part of Lustre, http://www.lustre.org/
+ *
+ * DebugFS for EFA Lustre Network Driver
+ *
+ * Author: Timothy Day <timday@amazon.com>
+ */
+
+#define DEBUG_SUBSYSTEM S_LND
+
+#include <linux/inet.h>
+#include <linux/debugfs.h>
+#include <lprocfs_status.h>
+
+#include "kcompat.h"
+#include "efalnd.h"
+
+struct dentry *kefalnd_debug_dir;
+
+static int gidmap_seq_show(struct seq_file *s, void *unused)
+{
+	struct kefa_peer_ni *peer_ni;
+	char ip_str[INET_ADDRSTRLEN];
+	struct rhashtable_iter iter;
+	u16 *gid_raw;
+
+	rcu_read_lock();
+	if (kefalnd.shutdown || kefalnd.init_state == EFALND_INIT_NONE) {
+		rcu_read_unlock();
+		return 0;
+	}
+
+	rhashtable_walk_enter(&kefalnd.peer_ni, &iter);
+	rhashtable_walk_start(&iter);
+
+	while ((peer_ni = rhashtable_walk_next(&iter)) != NULL) {
+		if (IS_ERR(peer_ni))
+			continue;
+
+		gid_raw = (u16 *)peer_ni->gid.raw;
+
+		snprintf(ip_str, INET_ADDRSTRLEN, "%d.%d.%d.%d",
+			 peer_ni->remote_nid_addr >> 24,
+			 peer_ni->remote_nid_addr >> 16 & 0xFF,
+			 peer_ni->remote_nid_addr >> 8 & 0xFF,
+			 peer_ni->remote_nid_addr & 0xFF);
+		seq_printf(s, "%-15s %04x:%04x:%04x:%04x:%04x:%04x:%04x:%04x %i %x\n",
+			   ip_str, be16_to_cpu(gid_raw[0]),
+			   be16_to_cpu(gid_raw[1]),
+			   be16_to_cpu(gid_raw[2]),
+			   be16_to_cpu(gid_raw[3]),
+			   be16_to_cpu(gid_raw[4]),
+			   be16_to_cpu(gid_raw[5]),
+			   be16_to_cpu(gid_raw[6]),
+			   be16_to_cpu(gid_raw[7]),
+			   peer_ni->cm_qp.qp_num,
+			   peer_ni->cm_qp.qkey);
+	}
+
+	rhashtable_walk_stop(&iter);
+	rhashtable_walk_exit(&iter);
+	rcu_read_unlock();
+
+	return 0;
+}
+LDEBUGFS_SEQ_FOPS_RO(gidmap);
+
+void kefalnd_debugfs_init(void)
+{
+	kefalnd_debug_dir = debugfs_create_dir("kefalnd", NULL);
+	if (IS_ERR_OR_NULL(kefalnd_debug_dir))
+		return;
+
+	debugfs_create_atomic_t("peerni_count", 0444, kefalnd_debug_dir,
+				&kefalnd.peer_ni_count);
+	debugfs_create_file("gidmap", 0444, kefalnd_debug_dir, NULL,
+			    &gidmap_fops);
+}
+
+void kefalnd_debugfs_exit(void)
+{
+	debugfs_remove_recursive(kefalnd_debug_dir);
+}
diff --git a/net/lnet/lnet/klnds/efalnd/efalnd_modparams.c b/net/lnet/lnet/klnds/efalnd/efalnd_modparams.c
--- a/net/lnet/lnet/klnds/efalnd/efalnd_modparams.c
+++ b/net/lnet/lnet/klnds/efalnd/efalnd_modparams.c
@@ -1,7 +1,7 @@
 // SPDX-License-Identifier: GPL-2.0
 
 /*
- * Copyright (c) 2023-2024, Amazon and/or its affiliates. All rights reserved.
+ * Copyright (c) 2023-2025, Amazon and/or its affiliates. All rights reserved.
  * Use is subject to license terms.
  */
 
@@ -98,6 +98,9 @@ kefalnd_tunables_setup(struct lnet_ni *ni)
 	if (net_tunables->lct_peer_tx_credits > EFALND_CREDITS_MAX)
 		net_tunables->lct_peer_tx_credits = EFALND_CREDITS_MAX;
 
+	if (net_tunables->lct_peer_timeout < EFALND_MIN_INIT_CONN_TIMEOUT)
+		net_tunables->lct_peer_timeout = EFALND_MIN_INIT_CONN_TIMEOUT;
+
 	if (net_tunables->lct_peer_tx_credits >
 	    net_tunables->lct_max_tx_credits)
 		net_tunables->lct_peer_tx_credits =
diff --git a/net/lnet/lnet/klnds/efalnd/efalnd_peerni.c b/net/lnet/lnet/klnds/efalnd/efalnd_peerni.c
--- a/net/lnet/lnet/klnds/efalnd/efalnd_peerni.c
+++ b/net/lnet/lnet/klnds/efalnd/efalnd_peerni.c
@@ -1,7 +1,7 @@
 // SPDX-License-Identifier: GPL-2.0
 
 /*
- * Copyright (c) 2023-2024, Amazon and/or its affiliates. All rights reserved.
+ * Copyright (c) 2023-2025, Amazon and/or its affiliates. All rights reserved.
  * Use is subject to license terms.
  */
 
@@ -84,6 +84,7 @@ static void peer_ni_free(struct kref *ref)
 		rhashtable_remove_fast(&kefalnd.peer_ni, &peer_ni->linkage,
 				       peer_ni_params);
 
+	atomic_dec(&kefalnd.peer_ni_count);
 	LIBCFS_FREE_PRE(peer_ni, sizeof(*peer_ni), "kfreed");
 	kfree_rcu(peer_ni, rcu_read);
 	rcu_read_unlock();
@@ -120,7 +121,7 @@ kefalnd_lookup_or_create_peer_ni(u32 nid_addr, union ib_gid *gid, u16 cm_qpn, u3
 {
 	struct kefa_peer_ni *new_peer_ni, *old_peer_ni;
 
-	LIBCFS_ALLOC_PTR(new_peer_ni);
+	CFS_ALLOC_PTR(new_peer_ni);
 	if (!new_peer_ni)
 		return ERR_PTR(-ENOMEM);
 
@@ -134,7 +135,7 @@ kefalnd_lookup_or_create_peer_ni(u32 nid_addr, union ib_gid *gid, u16 cm_qpn, u3
 	rcu_read_lock();
 	if (kefalnd.shutdown || kefalnd.init_state == EFALND_INIT_NONE) {
 		rcu_read_unlock();
-		LIBCFS_FREE_PTR(new_peer_ni);
+		CFS_FREE_PTR(new_peer_ni);
 		return ERR_PTR(-ENODEV);
 	}
 
@@ -147,7 +148,7 @@ kefalnd_lookup_or_create_peer_ni(u32 nid_addr, union ib_gid *gid, u16 cm_qpn, u3
 		       libcfs_nid2str(nid_addr));
 
 		rcu_read_unlock();
-		LIBCFS_FREE_PTR(new_peer_ni);
+		CFS_FREE_PTR(new_peer_ni);
 		return old_peer_ni;
 	}
 
@@ -159,11 +160,12 @@ kefalnd_lookup_or_create_peer_ni(u32 nid_addr, union ib_gid *gid, u16 cm_qpn, u3
 			old_peer_ni =  ERR_PTR(-ENODEV);
 
 		rcu_read_unlock();
-		LIBCFS_FREE_PTR(new_peer_ni);
+		CFS_FREE_PTR(new_peer_ni);
 		return old_peer_ni;
 	}
 
 	rcu_read_unlock();
+	atomic_inc(&kefalnd.peer_ni_count);
 	return new_peer_ni;
 }
 
@@ -208,6 +210,7 @@ kefalnd_find_remote_peer_ni(struct kefa_dev *efa_dev, struct lnet_nid *efa_nid)
 	struct lnet_nid_metadata *mapping;
 	lnet_nid_t tcp_nid4;
 	lnet_nid_t efa_nid4;
+	union ib_gid gid;
 	u32 nid_addr;
 	int rc = 0;
 	int i = 0;
@@ -237,8 +240,8 @@ kefalnd_find_remote_peer_ni(struct kefa_dev *efa_dev, struct lnet_nid *efa_nid)
 
 	rc = lnet_discover_nid_metadata(tcp_nid4, mapping);
 	if (rc) {
-		EFA_DEV_ERR(efa_dev, "Failed to ping TCP peer NI[%s]\n",
-			    libcfs_nid2str(tcp_nid4));
+		EFA_DEV_DEBUG(efa_dev, "Failed to ping TCP peer NI[%s]\n",
+			      libcfs_nid2str(tcp_nid4));
 		GOTO(out_mapping, rc);
 	}
 
@@ -259,10 +262,10 @@ kefalnd_find_remote_peer_ni(struct kefa_dev *efa_dev, struct lnet_nid *efa_nid)
 			continue;
 
 		kefa_nid_md = (struct kefa_nid_md_entry *)&mapping->nid_mappings[i];
+		memcpy(gid.raw, &kefa_nid_md->gid, sizeof(kefa_nid_md->gid));
 
 		new_peer_ni = kefalnd_lookup_or_create_peer_ni(LNET_NIDADDR(kefa_nid_md->nid),
-							       &kefa_nid_md->gid,
-							       kefa_nid_md->qp_num,
+							       &gid, kefa_nid_md->qp_num,
 							       kefa_nid_md->qkey);
 
 		if (IS_ERR_OR_NULL(new_peer_ni))
@@ -289,7 +292,6 @@ kefalnd_find_remote_peer_ni(struct kefa_dev *efa_dev, struct lnet_nid *efa_nid)
 	LIBCFS_FREE(mapping, mapping_size);
 
 out_error:
-	EFA_DEV_ERR(efa_dev, "Failed to perform ping\n");
 	RETURN(ERR_PTR(rc));
 }
 
@@ -311,7 +313,7 @@ int kefalnd_get_nid_metadata(struct lnet_ni *ni, struct lnet_nid_md_entry *md_en
 	if (kefalnd.shutdown || kefalnd.init_state == EFALND_INIT_NONE)
 		return -ENODEV;
 
-	kefa_ni_md->gid = efa_dev->gid;
+	memcpy(&kefa_ni_md->gid, efa_dev->gid.raw, sizeof(efa_dev->gid.raw));
 	kefa_ni_md->qp_num = efa_dev->cm_qp->ib_qp->qp_num;
 	kefa_ni_md->qkey = efa_dev->cm_qp->qkey;
 
@@ -322,82 +324,6 @@ int kefalnd_get_nid_metadata(struct lnet_ni *ni, struct lnet_nid_md_entry *md_en
 	return 0;
 }
 
-static ssize_t gidmap_show(struct kobject *kobj, struct kobj_attribute *attr,
-			   char *buf)
-{
-	struct kefa_peer_ni *peer_ni;
-	char ip_str[INET_ADDRSTRLEN];
-	struct rhashtable_iter iter;
-	ssize_t cnt = 0;
-	u16 *gid_raw;
-
-	rcu_read_lock();
-	if (kefalnd.shutdown || kefalnd.init_state == EFALND_INIT_NONE) {
-		rcu_read_unlock();
-		return 0;
-	}
-
-	rhashtable_walk_enter(&kefalnd.peer_ni, &iter);
-	rhashtable_walk_start(&iter);
-
-	while ((peer_ni = rhashtable_walk_next(&iter)) != NULL) {
-		if (IS_ERR(peer_ni))
-			continue;
-
-		gid_raw = (u16 *)peer_ni->gid.raw;
-
-		snprintf(ip_str, INET_ADDRSTRLEN, "%d.%d.%d.%d",
-			 peer_ni->remote_nid_addr >> 24,
-			 peer_ni->remote_nid_addr >> 16 & 0xFF,
-			 peer_ni->remote_nid_addr >> 8 & 0xFF,
-			 peer_ni->remote_nid_addr & 0xFF);
-		cnt += sysfs_emit_at(buf, cnt,
-				     "%-15s %04x:%04x:%04x:%04x:%04x:%04x:%04x:%04x %i %x\n",
-				     ip_str,
-				     be16_to_cpu(gid_raw[0]),
-				     be16_to_cpu(gid_raw[1]),
-				     be16_to_cpu(gid_raw[2]),
-				     be16_to_cpu(gid_raw[3]),
-				     be16_to_cpu(gid_raw[4]),
-				     be16_to_cpu(gid_raw[5]),
-				     be16_to_cpu(gid_raw[6]),
-				     be16_to_cpu(gid_raw[7]),
-				     peer_ni->cm_qp.qp_num,
-				     peer_ni->cm_qp.qkey);
-	}
-
-	rhashtable_walk_stop(&iter);
-	rhashtable_walk_exit(&iter);
-	rcu_read_unlock();
-
-	return cnt;
-}
-
-static struct kobj_attribute gidmap = __ATTR_RO(gidmap);
-static struct kobject *efalnd_kobj;
-
-int kefalnd_peerni_init(void)
-{
-	int rc = 0;
-
-	efalnd_kobj = kobject_create_and_add("efalnd", kernel_kobj);
-	if (!efalnd_kobj)
-		return -ENOMEM;
-
-	rc = sysfs_create_file(efalnd_kobj, &gidmap.attr);
-	if (rc) {
-		CERROR("failed to create gidmap sysfs\n");
-		return rc;
-	}
-
-	return rc;
-}
-
-void kefalnd_peerni_exit(void)
-{
-	kobject_put(efalnd_kobj);
-}
-
 /**
  * kefalnd_create_efa_nid() - Get EFA NID from IP and PCI bus/devfn number.
  * @ip_addr: IP address of interface used for TCP ping.
diff --git a/net/lnet/lnet/klnds/efalnd/efalnd-proto.h b/net/lnet/lnet/klnds/efalnd/efalnd_proto.h
similarity index 71%
rename from net/lnet/lnet/klnds/efalnd/efalnd-proto.h
rename to net/lnet/lnet/klnds/efalnd/efalnd_proto.h
--- a/net/lnet/lnet/klnds/efalnd/efalnd-proto.h
+++ b/net/lnet/lnet/klnds/efalnd/efalnd_proto.h
@@ -1,7 +1,7 @@
 // SPDX-License-Identifier: GPL-2.0
 
 /*
- * Copyright (c) 2023-2024, Amazon and/or its affiliates. All rights reserved.
+ * Copyright (c) 2023-2025, Amazon and/or its affiliates. All rights reserved.
  * Use is subject to license terms.
  */
 
@@ -40,11 +40,10 @@ enum kefa_comp_status {
 
 struct kefa_nid_md_entry {
 	lnet_nid_t nid;
-	union ib_gid gid;
-	__u16 qp_num;
-	__u32 qkey;
-	__u16 buffer_2;
-	__u64 buffer_end;
+	u8 gid[16];
+	u16 qp_num;
+	u32 qkey;
+	u8 buffer[10];
 } __packed;
 
 struct kefa_qp_proto {
@@ -52,33 +51,44 @@ struct kefa_qp_proto {
 	u32 qkey;
 } __packed;
 
-struct kefa_rdma_frag {
-	u64 addr;				/* CAVEAT EMPTOR: misaligned!! */
-	u32 nob;				/* # bytes this frag */
-} __packed;
-
 struct kefa_rdma_desc {
 	u32 key;				/* local/remote key */
-	struct kefa_rdma_frag frag;		/* buffer frag */
+	u64 addr;
+	u32 nob;				/* # of bytes */
 } __packed;
 
-
 struct kefa_immediate_msg {
 	struct lnet_hdr_nid4 hdr;		/* portals header */
 	char payload[0];			/* piggy-backed payload */
 } __packed;
 
+struct kefa_immediate_msg_v2 {
+	struct lnet_hdr_nid16 hdr;		/* portals header */
+	char payload[0];			/* piggy-backed payload */
+} __packed;
+
 struct kefa_putr_req_msg {
 	struct lnet_hdr_nid4 hdr;		/* portals header */
 	u64 cookie;				/* opaque completion cookie */
 	struct kefa_rdma_desc rdma_desc;	/* src rdma desc */
 } __packed;
 
+struct kefa_putr_req_msg_v2 {
+	struct lnet_hdr_nid16 hdr;		/* portals header */
+	u64 cookie;				/* opaque completion cookie */
+	struct kefa_rdma_desc rdma_desc;	/* src rdma desc */
+} __packed;
+
 struct kefa_getr_req_msg {
 	struct lnet_hdr_nid4 hdr;		/* portals header */
 	u64 sink_cookie;			/* opaque completion cookie */
 } __packed;
 
+struct kefa_getr_req_msg_v2 {
+	struct lnet_hdr_nid16 hdr;		/* portals header */
+	u64 sink_cookie;			/* opaque completion cookie */
+} __packed;
+
 struct kefa_getr_ack_msg {
 	u64 sink_cookie;			/* opaque completion cookie */
 	u64 src_cookie;				/* opaque completion cookie */
@@ -87,7 +97,7 @@ struct kefa_getr_ack_msg {
 
 struct kefa_completion_msg {
 	u64 cookie;				/* opaque completion cookie */
-	s16 status;				/* < 0 failure: >= 0 length */
+	s16 status;				/* enum kefa_comp_status */
 } __packed;
 
 struct kefa_conn_probe_msg {
@@ -100,9 +110,11 @@ struct kefa_conn_probe_msg {
 
 struct kefa_conn_probe_resp_msg {
 	u16 lnd_ver;				/* LND version */
-	s16 status;				/* < 0 failure: = 0 success */
+	s16 status;				/* enum kefa_comp_status */
 	u64 src_epoch;				/* Responder's epoch */
 	u64 caps;				/* Capabilities bit array */
+	u8 min_proto_ver;			/* Min supported protocol version */
+	u8 max_proto_ver;			/* Max supported protocol version */
 } __packed;
 
 struct kefa_conn_req_msg {
@@ -124,18 +136,13 @@ struct kefa_conn_req_ack {
 	u64 src_epoch;				/* Responder's epoch */
 	u64 caps;				/* Capabilities bit array */
 	u64 reserved;
-	s16 status;				/* < 0 failure: = 0 success */
+	s16 status;				/* enum kefa_comp_status */
 	u32 src_conn_id;			/* Responder's connection ID */
 	u32 nqps;				/* Number of data QPs on the array */
 	struct kefa_qp_proto data_qps[0];	/* Data QPs array */
 } __packed;
 
-struct kefa_msg {
-	/* First 2 fields fixed FOR ALL TIME */
-	u32 magic;				/* I'm an efanal message */
-	u8 proto_ver;				/* this is my protocol version number */
-	u8 type;				/* efa msg type */
-	u16 nob;				/* # bytes in whole message */
+struct kefa_msg_v1 {
 	lnet_nid_t srcnid;			/* sender's NID */
 	lnet_nid_t dstnid;			/* destination's NID */
 	u64 dst_epoch;				/* destination's epoch */
@@ -156,10 +163,50 @@ struct kefa_msg {
 	/* No additional fields can be added after the union. */
 } __packed;
 
+struct kefa_msg_v2 {
+	struct lnet_nid srcnid;			/* sender's NID */
+	struct lnet_nid dstnid;			/* destination's NID */
+	u64 dst_epoch;				/* destination's epoch */
+	u32 dst_conn_id;			/* ID for fast connection retrieval */
+	u8 credits;				/* returned credits */
+
+	union {
+		struct kefa_immediate_msg_v2 immediate;
+		struct kefa_putr_req_msg_v2 putr_req;
+		struct kefa_getr_req_msg_v2 getr_req;
+		struct kefa_getr_ack_msg getr_ack;
+		struct kefa_completion_msg completion;
+		struct kefa_conn_probe_msg conn_probe;
+		struct kefa_conn_probe_resp_msg conn_probe_resp;
+		struct kefa_conn_req_msg conn_request;
+		struct kefa_conn_req_ack conn_request_ack;
+	} __packed u;
+	/* No additional fields can be added after the union. */
+} __packed;
+
+struct kefa_hdr {
+		/* First 3 fields fixed FOR ALL TIME */
+	u32 magic;				/* I'm an efanal message */
+	u8 proto_ver;				/* this is my protocol version number */
+	u8 type;				/* efa msg type */
+	u16 nob;				/* # bytes in whole message */
+} __packed;
+
+struct kefa_msg {
+	struct kefa_hdr hdr;
+	union {
+		struct kefa_msg_v1 msg_v1;
+		struct kefa_msg_v2 msg_v2;
+	} __packed;
+	/* No additional fields can be added after the union. */
+} __packed;
+
 #define EFALND_MSG_MAGIC LNET_PROTO_EFA_MAGIC	/* unique magic */
 
 #define EFALND_PROTO_VER_1	0x81
-#define EFALND_CURR_PROTO_VER	EFALND_PROTO_VER_1
+#define EFALND_PROTO_VER_2	0x82
+#define EFALND_MIN_PROTO_VER	EFALND_PROTO_VER_1
+#define EFALND_MAX_PROTO_VER	EFALND_PROTO_VER_1
 
 #define EFALND_MSG_RESERVED		0x00
 #define EFALND_MSG_CONN_PROBE		0x01	/* connection probe */
diff --git a/net/lnet/lnet/klnds/efalnd/kcompat.h b/net/lnet/lnet/klnds/efalnd/kcompat.h
--- a/net/lnet/lnet/klnds/efalnd/kcompat.h
+++ b/net/lnet/lnet/klnds/efalnd/kcompat.h
@@ -29,32 +29,3 @@ static inline int ibdev_to_node(struct ib_device *ibdev)
 	return dev_to_node(parent);
 }
 #endif
-
-#ifndef HAVE_SYSFS_EMIT_AT
-/**
- *	sysfs_emit_at - scnprintf equivalent, aware of PAGE_SIZE buffer.
- *	@buf:	start of PAGE_SIZE buffer.
- *	@at:	offset in @buf to start write in bytes
- *		@at must be >= 0 && < PAGE_SIZE
- *	@fmt:	format
- *	@...:	optional arguments to @fmt
- *
- *
- * Returns number of characters written starting at &@buf[@at].
- */
-static inline int sysfs_emit_at(char *buf, int at, const char *fmt, ...)
-{
-	va_list args;
-	int len;
-
-	if (WARN(!buf || offset_in_page(buf) || at < 0 || at >= PAGE_SIZE,
-		 "invalid sysfs_emit_at: buf:%p at:%d\n", buf, at))
-		return 0;
-
-	va_start(args, fmt);
-	len = vscnprintf(buf + at, PAGE_SIZE - at, fmt, args);
-	va_end(args);
-
-	return len;
-}
-#endif
\ No newline at end of file
diff --git a/net/lnet/lnet/lnet/api-ni.c b/net/lnet/lnet/lnet/api-ni.c
--- a/net/lnet/lnet/lnet/api-ni.c
+++ b/net/lnet/lnet/lnet/api-ni.c
@@ -1978,7 +1978,7 @@ lnet_ping_target_install_locked(struct lnet_ping_buffer *pbuf)
 	struct lnet_ni *ni;
 	struct lnet_net	*net;
 	struct lnet_ni_status *ns, *end;
-	struct lnet_nid_metadata *mapping;
+	struct lnet_nid_metadata *data;
 	int rc = 0;
 	int i = 0;
 
@@ -1986,7 +1986,7 @@ lnet_ping_target_install_locked(struct lnet_ping_buffer *pbuf)
 	ns = &pbuf->pb_info.pi_ni[0];
 	end = (void *)&pbuf->pb_info + pbuf->pb_nbytes;
 
-	LIBCFS_ALLOC(mapping,
+	LIBCFS_ALLOC(data,
 		     sizeof(__u32) + (sizeof(struct lnet_nid_md_entry) * lnet_interfaces_max));
 
 	list_for_each_entry(net, &the_lnet.ln_nets, net_list) {
@@ -2004,8 +2004,8 @@ lnet_ping_target_install_locked(struct lnet_ping_buffer *pbuf)
 				CERROR("Refusing to send more than %i mappings",
 				       lnet_interfaces_max);
 			} else if (lnd->lnd_get_nid_metadata) {
-				lnd->lnd_get_nid_metadata(ni, &mapping->nid_mappings[i]);
-				mapping->nid_mappings[i].nid = lnet_nid_to_nid4(&ni->ni_nid);
+				lnd->lnd_get_nid_metadata(ni, &data->nid_mappings[i]);
+				data->nid_mappings[i].nid = lnet_nid_to_nid4(&ni->ni_nid);
 				i++;
 			}
 
@@ -2023,8 +2023,8 @@ lnet_ping_target_install_locked(struct lnet_ping_buffer *pbuf)
 	}
 
 	/* Add NID -> GID mapping to the end of ping buffer */
-	mapping->num_nid_mappings = i;
-	memcpy((struct lnet_nid_metadata *)ns, mapping,
+	data->num_nid_mappings = i;
+	memcpy((struct lnet_nid_metadata *)ns, data,
 	       sizeof(__u32) + (sizeof(struct lnet_nid_md_entry) * i));
 
 	/* We (ab)use the ns_status of the loopback interface to
@@ -2038,7 +2038,7 @@ lnet_ping_target_install_locked(struct lnet_ping_buffer *pbuf)
 	}
 	LNET_PING_BUFFER_SEQNO(pbuf) =
 		atomic_inc_return(&the_lnet.ln_ping_target_seqno);
-	LIBCFS_FREE(mapping,
+	LIBCFS_FREE(data,
 		    sizeof(__u32) + (sizeof(struct lnet_nid_md_entry) * lnet_interfaces_max));
 }
 
@@ -4755,7 +4755,7 @@ static struct lnet_ni *LNetGetLocalNID(struct lnet_nid *peer_nid)
 	cpt = lnet_net_lock_current();
 	list_for_each_entry(net, &the_lnet.ln_nets, net_list) {
 		list_for_each_entry(ni, &net->net_ni_list, ni_netlist) {
-			if (nid_net_same(&ni->ni_nid, peer_nid)) {
+			if (nid_same_net(&ni->ni_nid, peer_nid)) {
 				lnet_net_unlock(cpt);
 				return ni;
 			}
@@ -4861,41 +4861,31 @@ LNetGetForce(lnet_nid_t self4, struct lnet_handle_md mdh,
  * TODO - The special NID -> GID mapping functionality should be locked
  * behind a ping feature flag for compatibility reasons.
  */
-int lnet_discover_nid_metadata(lnet_nid_t nid, struct lnet_nid_metadata *mapping)
+int lnet_discover_nid_metadata(lnet_nid_t nid, struct lnet_nid_metadata *data)
 {
+	signed long timeout = cfs_time_seconds(30);
+	struct lnet_nid_metadata *data_tmp;
+	lnet_nid_t src_nid = LNET_NID_ANY;
+	int n_ids = lnet_interfaces_max;
 	struct lnet_process_id id4 = {
 		.nid = nid,
 		.pid = LNET_PID_LUSTRE,
 	};
-	struct lnet_md md = { NULL };
-	struct ping_data pd = { 0 };
 	struct lnet_ping_buffer *pbuf;
-	struct lnet_processid id;
+	struct ping_data pd = { 0 };
 	struct lnet_nid nid_struct;
-	struct lnet_nid_metadata *end;
-	lnet_nid_t src_nid = LNET_NID_ANY;
-	signed long timeout = cfs_time_seconds(30);
-	int n_ids = lnet_interfaces_max;
+	struct lnet_md md = { 0 };
+	struct lnet_processid id;
 	int id_bytes;
 	int nob;
-	int rc;
 	int rc2;
+	int rc;
 
 	lnet_nid4_to_nid(nid, &nid_struct);
 
 	CDEBUG(D_NET, "handshake with NID: %s\n",
 	       libcfs_nidstr(&nid_struct));
 
-	/* If the NID isn't TCP, this ping doesn't need to happen.
-	 * So, exit early.
-	 */
-	if (LNET_NETTYP(LNET_NIDNET(nid)) != SOCKLND) {
-		CDEBUG(D_NET, "refusing to handshake with NID: %s\n",
-		       libcfs_nidstr(&nid_struct));
-
-		return -1;
-	}
-
 	/* n_ids limit is arbitrary */
 	if (id4.nid == LNET_NID_ANY) {
 		CDEBUG(D_NET, "refusing to handshake LNET_NID_ANY\n");
@@ -4945,7 +4935,7 @@ int lnet_discover_nid_metadata(lnet_nid_t nid, struct lnet_nid_metadata *mapping
 	}
 
 	if (!pd.replied) {
-		CERROR("misc ping error\n");
+		CDEBUG(D_NET, "misc ping error\n");
 		rc = -EIO;
 		goto fail_ping_buffer_decref;
 	}
@@ -4994,17 +4984,17 @@ int lnet_discover_nid_metadata(lnet_nid_t nid, struct lnet_nid_metadata *mapping
 	}
 
 	/* Extract the mapping from the ping buffer we received */
-	end = (void *)&pbuf->pb_info.pi_ni[n_ids];
-	if (end->num_nid_mappings == 0 ||
-	    end->num_nid_mappings > lnet_interfaces_max) {
+	data_tmp = (struct lnet_nid_metadata *)&pbuf->pb_info.pi_ni[n_ids];
+	if (data_tmp->num_nid_mappings == 0 ||
+	    data_tmp->num_nid_mappings > lnet_interfaces_max) {
 		CERROR("%s: Unexpected number of handshake entries %i\n",
-		       libcfs_idstr(&id), end->num_nid_mappings);
+		       libcfs_idstr(&id), data_tmp->num_nid_mappings);
 		goto fail_ping_buffer_decref;
 	}
 
 	rc = 0;
-	memcpy(mapping, end,
-	       sizeof(__u32) + (sizeof(struct lnet_nid_md_entry) * end->num_nid_mappings));
+	memcpy(data, data_tmp,
+	       sizeof(__u32) + (sizeof(struct lnet_nid_md_entry) * data_tmp->num_nid_mappings));
 
 	CDEBUG(D_NET, "ping reply received with mapping\n");
 
